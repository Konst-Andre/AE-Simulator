# AE-Simulator · самері S40 · «E1 ВІТРИНА»

живе доки: Х22 (E1 у продукті) не пройшов device-тест
сусіднє живе самері: `AE_Simulator_session_summary_S39_E6_U_PRODUKTI.md` — **НЕ витіснене**:
його борги §0.1 · §0.3 (В-17…В-24) · §0.4 · §0.5 і правила П1…П9 (§4) лишаються чинними
там і сюди не переписуються. Тут — лише нове з S40. Стеля 2 на продукт дотримана.

Робочі файли: `index.html` продукту (робоче дерево `/mnt/user-data/outputs/AE/`, у репо
**не запушений**; відтворюється з `main` — §5) · `mockup_X21_v1.html` (не мінявся).

---

## §0 ВІДКРИТЕ

### 0.1 Наступний крок: Х22.3 — підключення E1

E1 **побудований і доведений, але не підключений** (П12 нижче). Х22.3 — один предмет
«вхід у E1», чотири частини:
1. `V2.picker = Training` (рядок після маркера `/* ── /v2 E1 ── */`, через x22_apply).
2. Низ панелі (`.side-bottom`, форма з макета) — два мости `/* v2-міст */`:
   «Налаштування» (старий Settings; через нього ж вхід у куратора) і «Змінити ім’я»
   (`localStorage.removeItem('ae_me'); S.me=null; S.screen='gate'`). **Вирок оператора S40.**
   Панель спільна → мости з'являться і на E6: П8 для E6 потребуватиме патча низу панелі.
   Потрібен символ `i-arrow` (у макеті є) — спрайт + X21 S6.
3. Банер «Модель не підключена» — над заголовком E1, лише за умови старого Picker
   `!MOCK && !KEY.get() && !S.cfg.model.workerReady`; клас старий `.err` (міст). **Вирок S40.**
4. **Міграція 4 смоуків продукту**, прибитих до DOM старого Picker (грепом S40):
   `smoke_ui_v1` :99 · :112–119 · :128 · :242–255 (`.card`=43, `.tabs button`=7, клік `.card`,
   «Налаштування») · `smoke_edit_ui_v1` :362 (клік «Налаштування») · `smoke_step1` :42–97
   (`.card`, `.tabs`, `.btn` «Почати зміну») · `smoke_step2` :232–235 (`.card`).
   Кожне твердження: переписати на DOM E1 (`.scenario-item`, `#e1-group`, `#start-shift`,
   `[data-start-one]`, міст «Налаштування») **або** назвати мертвим. `[джерело]`-твердження
   про `.tabs` у старому CSS лишаються правдою — `.tabs` живе далі в Settings.
   Норматив після міграції — нове число з прогону, не старе (12.1).
- ⚠ Доля коду `Picker`: рекомендація — **лишити** запасним (render бере його, коли
  `V2.picker` немає) до пушу й device-вироку; видалити на кроці 8. Рішення — у плані Х22.3.
- ⚠ Бюджет: міграція смоуків важка — Х22.3 почати зі свіжого чату, план і мікроскоп першим ходом.

### 0.2 Вирок В-25 — дописати в `SPEC §13.3` (разом із боргом S39 §0.3)

> **Ряд чіпів допустимий, лише коли на найвужчій ширині гейта (1280) вміщується в ОДИН
> ряд. Інакше — нативний селектор: поруч із пошуком, коли колонка широка (E6), або під
> пошуком на всю ширину, коли колонка вузька (E1). Кількість значень — не критерій,
> критерій — ширина.** В-21 уточнюється, не скасовується.

Підстава (замір рушієм, S40): E1, 7 груп, колонка списку 350 px на 1280 → **3 ряди чіпів**,
перший рядок списку на y 335, видно **5** карток; 1536/1920 — 2 ряди. Після селектора:
перший рядок на y **252** на всіх трьох ширинах, видно **6 · 8 · 11** карток (вікно
720/864/1080 без хрому браузера). Скрін оператора з демо — підстава вироку.
Також дописати до `§9 E1`: «Фільтр по `grp` — селектор з лічильниками, фасетними в межах пошуку».

### 0.3 Пуш (за оператором, S39 §0.4 розширюється)

Їде `index.html` = `main` + `x21_apply_v3` + `x22_apply_v3` (після Х22.3 — нова редакція).
У момент пушу: підняти `config.build`, дописати `ДЛЯ_ОЛІ/ЖУРНАЛ_ЗМІН.md` («що змінилось для
неї»: новий екран «Тренування» — список і довідка, група вибирається списком). Розбіжність
журналу `v10` проти збірки `v30` (S39 §0.4) — **досі не розібрана**.

### 0.4 Кандидат інструмента (не зроблено)

`ae_bundle --width 1920|1536|1280` (S39 §0.1) — рамка превʼю досі 1280. Не блокує.

---

## §1 Що зроблено · S40

| хід | предмет | чим доведено | device |
|---|---|---|---|
| — | підготовка: validator з raw, jsdom, Chromium 141; Х21 відтворено з `main` | `smoke_X21_v4` ✓32, inject 21 | — |
| — | звірка Project з обома репо (імʼя + вміст) → список на прибирання (§5) | лістинг zip репо, md5 | — |
| Х22.1 | CSS E1: проба `v2_probe_v2.js` (E6 6 станів ∪ E1 7) → порт `v2css_port_v2.py`; 117 блоків (було 70), усі 77 рядків E6 збережені; двійники `.tag/.trap` → `.v2-tag/.v2-trap` + гард зіткнень | старі екрани й E6 піксель-у-піксель: Х21↔Х22 15/15, main↔Х22 9/9; контроль інʼєкцією ✗ | — |
| Х22.2 | екран `Training()` (x22_training): список + довідка, `potential()` продукту, noSale «без пропозиції»/«—», `startOne`/`startShift` окремо, стан `S.e1` + `S.group`; спрайт +target/chat/cart | П8 проти макета 21/21 (7 станів × 3 ширини) з патчами П-а/б/в; поведінка в Chromium (фокус пошуку, дві дії, повернення зберігає вибір, 0 позицій без каталогу) | демо ✓ «чудова робота» + вирок чіпів |
| Х22.3а | група — нативний селектор під пошуком (В-25); блок `<style id="v2-css-own">` (власні правила, не з макета) | П8 з патчем П-г 21/21; інʼєкція 1 px → ✗7; замір рядів §0.2 | ⏳ демо `AE_preview_X22_3a_demo_v1.html` |

**Помилки, спіймані гардами, не оком:** (1) гард зіткнень класів порту рахував класи з
коментаря шапки → файл не записався; виправлено відкиданням коментарів, `.wrap` — названий
міст. (2) той самий гард назвав `list-tools` зіткненням, коли власний блок звався `v2-own` —
він рахує «старим» усе, крім `v2-css*`; блок перейменовано `v2-css-own`. (3) `smoke_X21` S5b
(реконструкція до `main`) не знав власного блоку → v6.

**Шум рушія (П11):** контроль «файл проти себе» дав ✗ у 8 з 18 знімків — GPU-растр.
Цифри Х22.1 були отримані на шумному растрі; шум дає лише хибні ✗, тож ✓ чесні, і всі
перепрогнані на детермінованому: 24/24.

---

## §2 Стан гейтів · прогоном 10.09.2026 (робоче дерево = main + x21_apply_v3 + x22_apply_v3)

- **Х21:** `node smoke_X21_v6.js <AE/index.html> --base <index.html з main>` → **✓32 · ✗0**;
  `--inject` → **усі 21**. (v4 → v5: S4 на порт v2; v5 → v6: S5b знімає `v2-css-own`.)
- **Продукт:** smoke_ui ✓112 · edit_ui ✓61 · step1 ✓28 · step2 ✓73 · rules ✓18 · ae_validate ✓19·⚠1
  (E1 не підключений — тому незмінні; після Х22.3 зміняться, §0.1.4).
- **Превʼю** `AE_preview_X22_3a_v1.html`: smoke_bundle ✓29 · inject 8/8 · Lens_validate ✓8·⚠2·✗0.
- **Макет:** X14 ✓30 · X15 ✓51 · X16 ✓54 · X17 ✓40 (не мінявся).
- **Рушій:** `python3 px_x22_1.py` → ✗0 (старі екрани й E6: Х21↔Х22 × 5 екранів, main↔Х22 × 3,
  на 1280/1536/1920); `python3 px_x22_3a.py` → ✗0 (E1 проти макета, 7 × 3).
  Обидва скрипти чекають бандли в `/home/claude/` (b_main · b_x21 · b_x22 з ae_bundle без
  `--desktop`) і `mock.html` = копія макета — підготувати так само, як у S40.
- Смоукам продукту потрібен `NODE_PATH` на `node_modules` (симлінк в outputs не працює).

---

## §3 Знахідки

- Формула орієнтира в макеті (`Σ bonus(bv)`, noSale → 0) ≠ продукту (`ideal − order`,
  мін. 1, `cfg.rule`): розходиться **31 картка з 40**. Беремо продукт (§11.1); у звірці — П-б.
- Живі сценарії = макет по всіх 43 полях, якщо `no` → `'#'+no`, а `character` → перша
  літера велика (усі 43 збігаються з мітками макета без урахування регістру).
- `.search-row` E6 зашитий під `.wide-table-card` — на E1 не діє; тому власне правило (§4 П13).

---

## §4 Правила й патерни S40 (повним текстом, 14.29)

**П10 · Двійники класів.** *Дім: патерн → `Lens_cookbook_delta_running.md` (поруч із П9).*
> Корінь `.v2` захищає старе від нового, але **не нове від старого без кореня**: старе
> `.tag{margin-left:6px}` протече в новий екран. Тому при переносі класи, що збігаються з
> іменами старого CSS, перейменовуються **явною мапою** в генераторі (`RENAME`), а генератор
> **падає** на будь-якому зіткненні, крім названих мостів. Класи рахуються без коментарів.

**П11 · Піксельна звірка — лише на детермінованому растрі.** *Дім: патерн → cookbook-дельта (до П8).*
> Headless Chromium з GPU-растром дає ±1 кольору на згладжуванні заокруглених кутів між
> прогонами **того самого файла** (S40: 8 з 18 знімків червоні). Запуск:
> `--disable-gpu --disable-gpu-rasterization --disable-partial-raster --force-color-profile=srgb --disable-lcd-text`,
> кожен стан — зі свіжого завантаження з накопиченими кроками, `animations='disabled'`.
> Перед вироком — **два контролі**: «файл проти себе» = 0 ✗ і інʼєкція (1 px) = ✗.

**П12 · Побудова ≠ підключення.** *Дім: протокол → `wsd_delta_running.md`.*
> Екран спершу будується **непідключеним** (функція є, маршрут на неї — ні) і доводиться
> звіркою: гейти продукту не рухаються. Підключення — окремий крок, і **смоуки старого
> екрана мігрують тим самим кроком**: кожне їхнє твердження або переписане, або назване
> мертвим. Інакше підключення валить гейти, і червоне змішується з регресом.

**П13 · Власні правила v2 — окремим блоком `v2-css-own`.** *Дім: патерн → cookbook-дельта.*
> Правило, якого немає в макеті (свідоме відхилення — В-25), не дописується в генерований
> блок (його S4 звіряє з портом байт-у-байт). Воно живе в `<style id="v2-css-own">` одразу
> після `v2-css`, пишеться руками з шапкою «живе доки», селектори з `.v2`. Префікс `v2-css`
> в id — умова гарда зіткнень порту. У П8 те саме правило входить у патч макета (П-г).
> Коли макет отримає це відхилення — правило переїжджає в порт.

---

## §5 Файли

| файл | куди | стан |
|---|---|---|
| `AE_Simulator_session_summary_S40_E1_VITRYNA.md` | Project | живе самері |
| `x22_apply_v3.py` · `x22_training_v2.js` · `x22_own_v1.css` | Project (вручну) до пушу Х22 | живі |
| `v2_probe_v2.js` · `v2css_port_v2.py` | Project (вручну) до кроку 8 | живі |
| `smoke_X21_v6.js` | Project (вручну) до кроку 8 | живий; **v4 видалити** |
| `px_x22_1.py` · `px_x22_3a.py` | Project (вручну) до device Х22 | живі (звірка рушієм) |
| `x21_apply_v3.py` · `e6_probe_v1.js` · `v2css_port_v1.py` | Project — лежать | **потрібні**: ланцюг x21_apply кличе порт v1 |
| `AE_preview_X22_3a_v1.html` · `…_demo_v1.html` | нікуди — похідне | одноразові |

**Робоче дерево в новій сесії** (усі інструменти й `mockup_X21_v1.html` в одній теці):
zip `main` → `outputs/AE` (29 файлів, без `archive/` і `mockup UI.zip`), далі
`node e6_probe_v1.js && python3 x21_apply_v3.py AE/index.html && node v2_probe_v2.js && python3 x22_apply_v3.py AE/index.html`.
Повторний запуск нічого не змінює (md5 звірено). Перевірка — `smoke_X21_v6` ✓32.

**Прибирання Project (список S40, звірено з обома репо):** видалити S35 · S36 (байт-у-байт в
AE `archive/summary/`) · `QR_Lens_…B59…` (у gov-архіві) · `Lens_…governance_E.md` (у
gov-архіві, байти не збігаються) · `smoke_X21_v4.js`. **Спершу в архів, потім видалити:**
S37 · S38 (у репо їх немає). Кандидати: `AE_Simulator.md` (вставлений чат Олі 01.09, ніхто
не посилається) · `README_AE_Simulator.md` (стара версія README). Не чіпати: X4…X13 (крок 8).
Помічено для governance: INDEX §5 оголошує EquipLens S30/S31, у Project лежать новіші S32–S34.

---

## §6 Стартове повідомлення наступної сесії

```
Мова спілкування — українська.

СТАРТ (wsd 1.1) — точково:
1. kernel/Lens_INDEX.md — маршрутизатор.
   ⚠ AE-Simulator у §5 НЕМАЄ і не буде — РІШЕННЯ, не борг.
2. AE_Simulator_session_summary_S40_E1_VITRYNA.md — живе самері, §0 зверху.
3. AE_Simulator_session_summary_S39_E6_U_PRODUKTI.md — лише §0.1 · §0.3 · §0.4 · §4
   (борги й правила П1–П9 живуть там, S40 їх не переписував).
4. AE_Simulator_SPEC_v0_4.md — §8.1 · §9 E1 · §11 · §13.5.
5. kernel/wsd/Work_Standard.md — точково за індексом.

⚠ В-24: судимо ЛИШЕ ПК-версію, CSS-ширини 1920 · 1536 · 1280. Телефон — лише перегляд.
⚠ В-25: чіпи — лише якщо на 1280 вміщуються в один ряд (S40 §0.2).
⚠ ГРЕП ПЕРЕД ТВЕРДЖЕННЯМ (12.1) — і для очікувань гейта: число з прогону.

ПІДГОТОВКА (перший хід, до плану):
· Lens_validate.py з raw: kernel/Lens_validate.py
· npm install jsdom (NODE_PATH на node_modules) ; Playwright+Chromium — промацати
· продукт: zip main → /mnt/user-data/outputs/AE (без archive/ і «mockup UI.zip»), потім
  node e6_probe_v1.js && python3 x21_apply_v3.py AE/index.html
  && node v2_probe_v2.js && python3 x22_apply_v3.py AE/index.html
  (якщо Х21/Х22 уже в main — відповідний крок НЕ робити)
· бандли для звірки рушієм: b_main · b_x21 · b_x22 (ae_bundle без --desktop) + mock.html

ГЕЙТИ:
· Х21:    node smoke_X21_v6.js <AE/index.html> --base <index.html з main> → ✓32 ; --inject → усі 21
· рушій:  px_x22_1.py → ✗0 ; px_x22_3a.py → ✗0 (П11: спершу «файл проти себе»)
· превʼю: ae_bundle -o AE_preview_X<хід>_v<n>.html --desktop ; smoke_bundle ✓29 ; --inject 8 ;
          Lens_validate --html ✓8 · ⚠2 · ✗0
· продукт: smoke_ui ✓112 · edit_ui ✓61 · step1 ✓28 · step2 ✓73 · rules ✓18 · ae_validate ✓19·⚠1
          (до Х22.3; після міграції — нові числа з прогону)
· макет:  X14 ✓30 · X15 ✓51 · X16 ✓54 · X17 ✓40

── ЗАВДАННЯ СЕСІЇ ──
Х22.3 — підключення E1: V2.picker, мости внизу панелі, банер, міграція 4 смоуків (S40 §0.1).
Першим ходом — план і МІКРОСКОП ПЛАНУ окремим блоком, до коду.
Звіт кожного ходу — першим рядком «що ти побачиш» (S39 §4 П6).

Довіряю, делегую партнере.
```
