# AE-Simulator · самері S44 · «РОЗМОВА»

живе доки: Х23.4 (скрол списку пропозицій, В-37) не пройшов device разом із наступним екраном
**витісняє S43** (`AE_Simulator_session_summary_S43_DIETA.md`): його §0.1 (E2) закрито тут — device ✓; решта
відкритого (§0.2–§0.6) і правила П10–П22 перенесені сюди **дослівно** (§0.2–§0.6, §4). S43 → `archive/summary/`.
Сусіднє живе самері — **S39** (його §0.1 · §0.3 В-17…В-24 · §0.5 і П1–П9 чинні там). Стеля 2 на продукт: S39 + S44.

**Робоча версія — `AE_WORK_index.html` у Project (вручну, В-33).** md5 `ee81b79ffa204e5543a5fa62c8a828eb`.
Рецепт дерева змінився — §5 (x22_4_apply **v4** замість v3 + x23_smokes_apply_v1).

---

## §0 ВІДКРИТЕ

### 0.1 Наступний крок — вибрати екран і першим ходом будови зробити Х23.4 (В-37)
E2 перенесено, device ✓ (S44). Наступний екран — **вибір на старті** (П22: перепитати, не наказ).
Кандидати §0.2: **E3** (найважливіший, зображення немає — малювати з SPEC §3.4; «До підсумку зміни» з E2 зараз
веде в старий `Result`) · E4 (таблиця сценаріїв).
**Х23.4 — вирок оператора S44 (В-37):** скрол **лише списку** «Запропонувати позицію» (зараз `.cart-panel
{overflow:auto}` скролить усю праву колонку разом із чеком — так малює макет). Обовʼязково в наступному чаті,
**привʼязати до першого ходу будови**, окремого ходу не виділяти. Умова асистента (прийнята): у звіті ходу —
**окремий рядок гейта**, щоб скрол і новий екран не судились одним поглядом (wsd 1.18). Власне правило поза
макетом → `v2-css-own` (П13); `panel` пропозицій — flex-колонка, список `min-height:0; overflow:auto`.

### 0.2 Решта екранів (SPEC §8) — з S43, дослівно, E2 викреслено
Звичайний: ~~E2~~ ✓S44 · E3 (зображення немає — малювати з §3.4) · E4 · E5 (бажане). Кураторський: A1 · A2 (редактор) ·
A3 · A4 · A6 · A7 — пишуть `data/*.json`, ціна помилки вища (SPEC §13.5). ~9 екранів.

### 0.3 Спека — дописати (борг S42/S43 дослівно + нове S44)
Із S43 §0.3 **без змін**: `SPEC §13.3` ← В-25 · В-26 · В-27 · В-28 · В-29 · В-30 · В-31 · В-32 · В-33 (тексти — у S43,
яке йде в архів: **перенести їх у спеку ДО видалення S43 з Project** або тягти raw з `archive/summary/`);
`§9 E1` — селектор з фасетними лічильниками; борг S39 §0.3 (В-17…В-24).

**Нові вироки S44 — у `SPEC §13.3` / `§9 E2`:**
- **В-34 (обрав рекомендацію асистента, Р1).** На E2 грошей немає: чек — назва, категорія, код; ні бонусу позиції,
  ні «Було · Стало · Приріст · Орієнтир» (`§9 E2`: «бонусів і сум фармацевт не бачить ніколи»). Приріст
  показується **лише в шапці розбору після кінця** («РОЗБІР · приріст X з Y ₴» — як і було в `Game`).
- **В-35 (обрав рекомендацію асистента, Р2).** Вкладки пропозицій — **дві**: «Для цієї ситуації» (відкрита першою)
  · «Усі товари» + пошук. «Популярні» знято: носія в даних немає, `f:⭐` = «рекомендована», не «популярна».
  Нове поле = дані + правило `ae_rules` + місце в редакторі (§13.1) — не зараз. `§9 E2` «три вкладки» → дві.
- **В-36 (делеговано, обрав асистент, Р3).** Кінець розмови макет не малює. «Завершити» макета (завжди → E3)
  суперечить §3.2 (кінець ухвалює клієнт) — не перенесено. На його місці в шапці **лише після кінця від клієнта**:
  «Пройти ще раз» (`.secondary`) + вихід (`.primary`): «До списку» · «Наступне замовлення» · в останній розмові
  зміни — «До підсумку зміни». Розбір — картка в стрічці діалогу; «Замовлення N з M» — тег у шапці.
- **В-37 (вирок оператора, device S44).** Скрол лише списку пропозицій — §0.1.

**Рішення асистента S44 (названі в шапці `Talk()`; вето оператора не накладено — вписати в `§9 E2`):**
«Для цієї ситуації» = `poolOf(sc)` (категорії замовлення — те, що бачить клієнт-модель), **не** `bv/bm`
(демо макета — це ключ відповіді) · під назвою позиції — категорія (`hint`/`pack` у даних немає) · час репліки —
штамп `t` у момент push (у модель ідуть лише `role/content`) · **брифінг без згортання — відхилення від §3.2**
(три колонки дають брифінгу власну висоту; згортання рятувало одну колонку) · `+` **дописує** назву до набраного,
не затирає (макет затирав) · тост — лише на `+` · підказку «Відповідайте своїми словами» знято (у макеті немає) ·
на E2 бічна панель — **лише бренд** (як макет: без навігації й дверей) · відповідь, що прийшла після виходу з
розмови, відкидається (`Game` писала її в чужу) · заготовки й `+` після кінця заглушені · `?mock`-панель і `?step`
лишились (міст: клас `.mock` старого CSS).

### 0.4 Фінальна передача Олі (В-31) — з S43, дослівно
- що їде: `index.html`, `config.json` (без `curatorCode`), `ДЛЯ_ОЛІ/ПОЧНИ_ЗВІДСИ.md`, смоуки `tools/`;
  `smoke_edit_ui` крутиться в CI (`perevirka.yml`) — без нього пуш зламає CI;
- ⚠ секрет `AE_EDIT_CODE` у Cloudflare — заведений і відомий Олі, інакше в налаштування не зайде ніхто;
- розбіжність журналу `v10` проти збірки `v30` (S39 §0.4) — **досі не розібрана**;
- у журнал: новий вигляд, екрана імені немає, кураторський режим — картка внизу панелі за кодом запису;
- **+ S44:** у журнал — нова розмова: три колонки, у чеку немає сум, приріст — у розборі.

### 0.5 Ідея оператора (дім тут, не зроблено) — з S43, дослівно
Пружинна анімація повзунка «вліво-вправо в межах свого контейнера» — задумана для варіанта Б (перемикач).
Варіант А її не має. Якщо повернемось до перемикача (разом із кураторською панеллю v2), то смисл такий: повзунок їде
**лише на прийнятий код**, перехід стартує після кінця анімації. Числа — тільки після device (1.17).

### 0.6 Запасні старі екрани
З S43: запасний `Picker`, кнопка «Налаштування» (:2563→ зсунулась) з охороною Settings повертає на список —
лишено свідомо, помирає з кроком 8 (Р1). **+ S44:** старий `Game` — так само запасний (render бере його, коли
`V2.game` немає); помирає в кроці 8.

### 0.7 Нерозібране S44
- `smoke_X21_v10` (поза гейтом, В-32) — прогін S44 з робочої теки: ✗2 «S7 · продукт не завантажився». **Причину
  не розібрано** (можливо, середовище запуску, можливо, правка старого коду `sidebar` у Х23.3). За В-32 він і так
  іде в архів «на першій правці старого коду в E2» — правка сталась (§5).
- `smoke_edit_ui --inject` дає ✓62·✗0, `ae_validate --inject` падає трасою Node — **так само до й після S44**, не регрес;
  що ці режими мали б робити — не розібрано.

---

## §1 Що зроблено · S44

| хід | предмет | чим доведено | device |
|---|---|---|---|
| 1 | підготовка (зіп `Konst-Andre/AE-Simulator@main`, md5 `2d11ad49` ✓, гейт = S43 §2) · план E2 + мікроскоп · розвилки Р1–Р3 | — | — |
| 2 | **Х23.1** CSS E2: `v2_probe_v3` (E2 8 станів + тост) · `v2css_port_v3` (`RENAME` + `back`, `tabs`) | 119 → 228 правил, зникло 0; нові проти DOM E1/E6/дверей — 0 влучань; контроль I-a/I-b — кожна своє; гейт без руху | — |
| 3 | **Х23.2** `Talk()` непідключений · спрайт `i-arrow i-info i-send` · `v2-css-own` Х23.2 | гейт і `--inject` дельта 0; 21 ціль інʼєкцій не зрушила; разова звірка ✓27 + контролі I-c/I-d | — |
| 4 | **Х23.3** підключення `V2.game = Talk` · панель-бренд · `.bubble` overflow-wrap · міграція 14 пар | гейт §2; `--inject` зведено поіменно; `smoke_bundle` ✓32 | ✓ |
| 5 | **Х23.3а** (device S44): заготовки й «1 позиція» переносяться на 1280 | гейт §2 без руху; bundle ✓32 | ✓ |
| 6 | рецепт §5: `x22_4_apply_v4` · доказ дерева з нуля | md5 `ee81b79f`, `diff -rq` порожній | — |

---

## §2 Стан гейтів · прогоном 11.09.2026 (`ee81b79f`)

**Гейт В-32 — 7 перевірок продукту** (з кореня `AE`, `NODE_PATH` на `node_modules`):
`tools/smoke_ui_v1.js` **✓122** · `tools/smoke_edit_ui_v1.js .` ✓62 · `tools/smoke_step1.js` **✓28** ·
`tools/smoke_step2.js` ✓73 · `tools/smoke_step3.js index.html` ✓29 · `tools/smoke_rules_v1.js` ✓18 ·
`tools/ae_validate.js` ✓19·⚠1.
**`--inject`:** `smoke_ui` ✗11 (було ✗14: − «настрій поряд із ціллю» − 4 згортання, мертві з переносом; + «ціль у
картці» — нова ціль інʼєкції 4; + «розбір від судді» — оживлений детектор, §3) · `step1` ✗1 (`bonusOf = 0` → шапка
розбору) · `step2` ✗16 (без змін).
**Превʼю:** `python3 ae_bundle_v1_3.py AE -o AE_preview_<хід>.html --desktop` → `smoke_bundle_v1_4` ✓32 · Lens_validate
✓8·⚠2·✗0. Для device розмови — **похідне превʼю-заглушка** (П27): у рамці `MOCK = true`, один рядок різниці.

---

## §3 Знахідки

- **Id-правило макета порт губить мовчки.** `#screen-e2 main { display:grid; grid-template-rows:auto minmax(0,1fr);
  overflow:hidden }` — єдине id-правило макета, і воно тримало всю висоту E2. Probe знімає id з кореня, а в продукті
  `#screen-e2` немає → ліковано класом `.talk` у `v2-css-own` (П23).
- **Спрайт продукту не мав `i-arrow · i-info · i-send`** — E2 малював би порожні кнопки. `x22_4_apply` колись
  вирізав `i-arrow` («більше ніхто не кличе») → той самий скрипт у рецепті шкодив би робочій версії → v4 (§5).
- **`.v2 .bubble` без `overflow-wrap:anywhere`** — старий урок «довгий токен» тримало твердження, що зеленіло на
  **старому** CSS. Правило додано, твердження переведено на v2.
- **Сліпий детектор «розбір прийшов від судді» — не ловив нічого й ніколи**: шукав рядок стаба в `d.body.textContent`,
  а там завжди лежить `<script>` зі стабом. Тепер дивиться в `.talk-review`, інʼєкція 2 його червонить (П24).
- **Промахи асистента S44:** (1) ряд заготовок перенесено як є — **В-21/В-25 не звірено в плані**, спіймав device
  (12.1); (2) у плані «17 цілей інʼєкцій» — підрахунок рядків грепом, заміряно генератором — 21; (3) 3 зіткнення
  класів у плані, насправді `.client` макет не стилізує — перейменування не треба; (4) гард інʼєкцій у
  `x23_apply` спершу рахував і нові цілі мігрованих смоуків (П25); (5) ланцюг команд з `;` після `&&` стер
  превʼю й v2 з outputs — втрат немає, але правило (П26); (6) базис `--inject` для порівняння спершу відтворено
  криво (замінено 3 файли замість дерева) — переробено з повного рецепта.
- **П16 спрацював на асистентові:** перша редакція твердження про гонку була зелена на інʼєкції (§4, доповнення П16).

---

## §4 Правила й патерни (повним текстом, 14.29)

**Перенесені з S40–S43 (не вписані) — дослівно:**

**П10 · Двійники класів.** *Дім: патерн → `Lens_cookbook_delta_running.md` (поруч із П9).*
> Корінь `.v2` захищає старе від нового, але **не нове від старого без кореня**: старе
> `.tag{margin-left:6px}` протече в новий екран. Тому при переносі класи, що збігаються з
> іменами старого CSS, перейменовуються **явною мапою** в генераторі (`RENAME`), а генератор
> **падає** на будь-якому зіткненні, крім названих мостів. Класи рахуються без коментарів.

**П11 · Піксельна звірка — лише на детермінованому растрі.** *Дім: патерн → cookbook-дельта (до П8).*
> Headless Chromium з GPU-растром дає ±1 кольору на згладжуванні заокруглених кутів між
> прогонами **того самого файла** (S40: 8 з 18 знімків червоні). Запуск:
> `--disable-gpu --disable-gpu-rasterization --disable-partial-raster --force-color-profile=srgb --disable-lcd-text`,
> кожен стан — зі свіжого завантаження з накопиченими кроками, `animations='disabled'`.
> Перед вироком — **два контролі**: «файл проти себе» = 0 ✗ і інʼєкція (1 px) = ✗.

**П12 · Побудова ≠ підключення.** *Дім: протокол → `wsd_delta_running.md`.*
> Екран спершу будується **непідключеним** (функція є, маршрут на неї — ні) і доводиться
> звіркою: гейти продукту не рухаються. Підключення — окремий крок, і **смоуки старого
> екрана мігрують тим самим кроком**: кожне їхнє твердження або переписане, або назване
> мертвим. Інакше підключення валить гейти, і червоне змішується з регресом.

**П13 · Власні правила v2 — окремим блоком `v2-css-own`.** *Дім: патерн → cookbook-дельта.*
> Правило, якого немає в макеті (свідоме відхилення — В-25), не дописується в генерований
> блок (його S4 звіряє з портом байт-у-байт). Воно живе в `<style id="v2-css-own">` одразу
> після `v2-css`, пишеться руками з шапкою «живе доки», селектори з `.v2`. Префікс `v2-css`
> в id — умова гарда зіткнень порту. У П8 те саме правило входить у патч макета (П-г).
> Коли макет отримає це відхилення — правило переїжджає в порт.

**П14 · Питання оператору — «чи потрібно», а не «куди».** *Дім: протокол → `wsd_delta_running.md`.*
> Пропозиція, що додає на екран елемент, якого немає в макеті, спершу звіряється з макетом і
> спекою, і розбіжність **називається в самому питанні**. Питання «куди подіти X» без «чи
> потрібен X» підсовує відповідь. Рядок «вирок оператора» в самері пишеться лише тоді, коли
> оператор сформулював рішення сам; якщо обрав рекомендацію — так і писати: «обрав рекомендацію
> асистента». *Анти-приклад S40→S41: два мости внизу панелі, записані як вирок, — хід роботи і
> звірки викинуто.*

**П15 · Правки старого коду — названим списком з одним домом.** *Дім: патерн → cookbook-дельта (поруч із П3).*
> Коли перенос мусить змінити код поза шаром v2 (екран імені), правки лежать списком
> `[назва, було, стало]` в одному файлі. Генератор застосовує їх («рівно один збіг»), звірка з
> `main` **відкочує** той самий список і лише потім порівнює. Неназвана правка поруч лишається
> червоною (I26). Два списки — у генераторі й у смоуку — розійшлися б мовчки.
> **Доповнення S42:** правка-видалення несе **якір** (рядок перед нею) і в «було», і в «стало»:
> порожнє «стало» відкотити нема чим. Вставка (було ⊂ стало) судиться «стало рівно раз», а не «було зникло».

**П16 · Інʼєкцію може поглинути захист продукту.** *Дім: протокол → `wsd_delta_running.md` (кандидат у 1.15).*
> Інʼєкція, наслідок якої продукт сам зводить назад до правильного стану (перехід у `edit` без
> замка → `settings`), дає «не спіймано» при чесному детекторі. Перш ніж лагодити детектор —
> перевірити, чи інʼєкція взагалі ламає спостережуване.
> **Доповнення S44:** поглинути може й **заглушка**. Зняли гард гонки (пізня відповідь після виходу) — але
> відповідь пишеться в захоплений масив, а зміну `S.cart/S.ended` першого ходу заглушка повертає тією самою
> (той самий чек, `ended=false`). Твердження дивилось на `S.history` — не туди. Пізньою зроблено 4-ту репліку
> (на ній заглушка завершує), твердження дивиться на `S.ended`/`S.cart` нової розмови.

**П17 · Детектор, що живе побічним шляхом, засинає разом із ним.** *Дім: протокол → `wsd_delta_running.md` (кандидат у 1.15).*
> I5 ловився не перевіркою сховища, а тим, що старт читав ім'я. Прибрали функцію — детектор
> осліп без жодного червоного. Коли з продукту йде функціональність, прогнати `--inject` усіх
> гейтів і для кожного «не спіймано» знайти, яким шляхом він ловився.

**П18 · Маркер блоку — унікальний на весь файл, ідемпотентність — з чистої бази.** *Дім: протокол → `wsd_delta_running.md` (кандидат у 1.15).*
> Генератор замінює блок «від маркера A до маркера B». Якщо A збігається з текстом деінде
> (шапка CSS-блоку = шапка JS-блоку), другий прогін бере перший збіг і вклеює код не туди.
> «md5 двох прогонів поспіль рівні» цього **не ловить**: стійкою точкою стає вже зіпсований стан.
> Маркери розводяться (`· JS` / `· CSS`), генератор має гард «кожен маркер рівно раз, у стилях
> немає коду». Ідемпотентність доводиться **прогоном на чистій базі** плюс повтором.
> Спіймала інʼєкція: фрагмент, що мав бути один, знайшовся двічі.
> **Доповнення S43 — вкладені пари не ідемпотентні.** Якщо нова пара правок міняє текст, який уставила
> інша пара того самого списку, повтор бачить «стало» першої пари зміненим і падає. Нове вшивається в «стало» тієї
> самої пари, а не окремою парою поверх. *(S44: так вшито `V2.game = Talk`.)*

**П19 · Мок відтворює правила середовища, а не лише відповіді сервера.** *Дім: патерн → cookbook-дельта (поруч із П4).*
> Мок `fetch`, що вміє 403/400, пропустив заголовок з некодованою кирилицею: браузер кидає
> TypeError «non ISO-8859-1» ще до мережі, а мок — ні. Тому I-d (без `encodeURIComponent`) була
> зеленою. У моку — те саме правило: символ поза ISO-8859-1 у заголовку → reject TypeError.

**П20 · Смоук під інʼєкцією не мусить падати від відсутнього вузла.** *Дім: протокол → `wsd_delta_running.md`, до П4.*
> Під I27 (403 пускає) двері відчиняються на першому коді, поля більше немає, і `typeGo`
> обвалював увесь прогін, ховаючи решту 26 інʼєкцій. Хелпер дії перевіряє, що вузол є, і
> мовчки не діє; червоніє **твердження**, а не смоук.

**П21 · Детектор формули проганяє кожну її гілку.** *Дім: протокол → `wsd_delta_running.md` (кандидат у 1.15).*
> Формула з розвилкою (`min`/`max`, «вужче / ширше») перевіряється на вході з **кожного** боку розвилки.
> Середовище смоуку має власне значення (jsdom: вікно 1024) і тихо обирає одну гілку — друга лишається
> неперевіреною, і стара вада живе під зеленим гейтом. Контроль: інʼєкція, що ламає лише другу гілку, мусить
> дати ✗ **лише** на її твердженні (S43: I9 → W4b).

**П22 · Етап із самері — не наказ, коли він зачіпає оператора.** *Дім: протокол → `wsd_delta_running.md`, поруч із П14.*
> «Наступний крок» самері, що вимагає дії або рішення оператора (пуш, показ людині, передача), на старті нової
> сесії **перепитується** («чи потрібно зараз»), а не пропонується як виконання. Самері пише минула сесія;
> обставини оператора могли змінитись. *Анти-приклад S43: пуш-пакет за S42 §0.3, скасований В-31.*

**Нові · S44:**

**П23 · Чекліст переносу екрана — два греби до плану (доповнення П9; оператор: «беремо»).** *Дім: патерн → cookbook-дельта, поруч із П9.*
> П9 відбирає CSS за влученням у DOM, і тому **мовчки губить id-правила** макета (`#screen-…`): probe знімає id з
> кореня, а в продукті такого id немає. Перед переносом екрана: (1) `грep '#[\w-]+[^{]*\{'` у CSS макета — кожне
> id-правило, що стосується екрана, переписати класом обгортки у `v2-css-own` (S44: `#screen-e2 main` → `.talk`,
> тримало всю висоту); (2) кожен ряд кнопок/чіпів макета звірити з **В-21/В-25** (ряд не скролиться; на 1280 — один
> ряд або перенос/селектор). *Анти-приклад S44: заготовки перенесено з `overflow:auto`, спіймав device.*

**П24 · `d.body.textContent` бреше, коли скрипти лежать у `body`.** *Дім: протокол → `wsd_delta_running.md` (кандидат у 1.15).*
> Твердження «текст X є на сторінці» через `body.textContent` зеленіє, якщо X трапляється в **коді** вбудованого
> `<script>` (стаб, заглушка, рядок повідомлення). Шукати в **вузлі екрана**, де текст мусить зʼявитись.
> Контроль — інʼєкція, що прибирає показ, мусить дати ✗ (S44: «розбір від судді» не ловив ніколи).

**П25 · Гард «цілі інʼєкцій не зрушили» судить лише цілі, що вже є в базі.** *Дім: патерн → cookbook-дельта, поруч із П18.*
> Генератор, що вставляє код **раніше** за старий, перевіряє: кількість збігів кожної цілі `html.replace('…')`
> смоуків та сама, що в базі (`replace` бере перший збіг — нова копія тексту перехопила б інʼєкцію). Після міграції
> смоуки несуть **нові** цілі (тексти нового коду) — у базі їх 0, це не зсув. Правило: `base.count(t) and
> base.count(t) != new.count(t)`.

**П26 · Деструктивна команда — окремим викликом, після успішних гейтів.** *Дім: протокол → `wsd_delta_running.md`.*
> Ланцюг `a && b; rm …` виконує `rm` навіть коли `a` впав. S44: генератор упав на гарді, а `rm` у тому ж рядку
> стер попереднє превʼю й генератор з outputs. `rm`/`mv`/перезапис робочих копій — окремим викликом, коли
> результат попереднього прочитано.

**П27 · Превʼю-заглушка — похідне з однією названою різницею.** *Дім: патерн → cookbook-дельта, поруч із П2.*
> Рамка `srcdoc` не має `?mock`, тож розмова в превʼю йде в живу модель без ключа — device бачить лише помилку.
> Похідний файл: `const MOCK = …` → `const MOCK = true` рівно раз, з коментарем «лише в цьому файлі». `smoke_bundle`
> судить **базове** превʼю (його A8 реконструює дім байт-у-байт), похідне — «різниця = 1 рядок».

---

## §5 Файли · накладна

**Project — додати / замінити:**
| файл | живе доки |
|---|---|
| `AE_Simulator_session_summary_S44_ROZMOVA.md` | живе самері (витісняє S43) |
| `AE_WORK_index.html` — **замінити** (md5 `ee81b79f…`, було `2d11ad49…`) | фінальний пуш (В-31); замінюється щоразу, коли змінився |
| `x22_4_apply_v4.py` | фінальний пуш — у рецепті **замість** v3 |
| `x23_smokes_apply_v1.py` | фінальний пуш — рецепт |
| `x23_apply_v3.py` | фінальний пуш — регенерація `index.html` з бази `0dd569d7` (у рецепті не потрібен: AE_WORK уже несе) |
| `v2_probe_v3.js` · `v2css_port_v3.py` | порт наступних екранів; крок 8 — **замінюють** v2 |

**Project — видалити:** `v2_probe_v2.js` · `v2css_port_v2.py` (замінені v3) · `x22_4_apply_v3.py` (замінений v4 у рецепті;
історія — у репо `archive/`, якщо треба) · `smoke_X21_v10.js` — за В-32 **в архів** (§0.7: ✗2 нерозібрано).
**Спершу в архів репо, потім видалити:** **S43** — ⚠ тексти В-25…В-33 тільки в ньому, доки не вписані в спеку (§0.3).
Лишки списків S40–S43 (досі не виконані): видалити S35 · S36 · `QR_Lens_…B59…` · `Lens_…governance_E.md`; S37 · S38 — спершу
в архів; кандидати — `AE_Simulator.md` · `README_AE_Simulator.md`. Із S43 §5: `x22_4_apply_v2.py` · `x22_4_door_v1.js` ·
`x22_4_own_v1.css` · `x22_4_smokes_apply_v1.py` · `ae_bundle_v1_2.py` · `smoke_bundle_v1_3.js` · `AE_door_compare_v1.html` ·
`px_x22_3b_v3.py` — перевірити, чи вже видалені.

**Лишаються (потрібні):** `x22_smokes_apply_v1.py` · `x22_4_smokes_apply_v2.py` · `x22_4_edits_v2.json` · `x22_4_door_v2.js` ·
`x22_4_own_v2.css` · `mockup_X21_v1.html` · `x22_own_v1.css` · `ae_bundle_v1_3.py` · `smoke_bundle_v1_4.js`.
**Лише історія (не запускаються):** `e6_probe_v1` · `x21_apply_v3` · `x22_apply_v6` · `x22_training_v4` · `x22_old_edits_v1` ·
X4…X17 — прополка в кроці 8.

**Робоче дерево в новій сесії** (доведено S44: md5 `ee81b79f`, `diff -rq` з робочим деревом порожній):
```
зіп codeload.github.com/Konst-Andre/AE-Simulator/zip/refs/heads/main → /mnt/user-data/outputs/AE
   (без archive/ і «mockup UI.zip»; лише python3 zipfile — unzip нівечить кирилицю)
cp /mnt/project/AE_WORK_index.html AE/index.html
python3 x22_4_apply_v4.py AE && python3 x22_smokes_apply_v1.py AE && python3 x22_4_smokes_apply_v2.py AE && python3 x23_smokes_apply_v1.py AE
```
Перевірка: md5 `index.html` = `ee81b79ffa204e5543a5fa62c8a828eb` (скрипти його не пишуть); гейт §2.
Шлях `/mnt/user-data/outputs/AE` зашитий у смоуках і `ae_bundle` — дерево мусить лежати там.

---

## §6 Стартове повідомлення наступної сесії

```
Мова спілкування — українська.

СТАРТ (wsd 1.1) — точково:
1. kernel/Lens_INDEX.md — маршрутизатор. ⚠ AE-Simulator у §5 НЕМАЄ — РІШЕННЯ, не борг.
2. AE_Simulator_session_summary_S44_ROZMOVA.md — живе самері, §0 зверху (S43 витіснене).
3. AE_Simulator_session_summary_S39_E6_U_PRODUKTI.md — лише §0.3 · §4 (П1–П9).
4. AE_Simulator_SPEC_v0_4.md — §3.3 · §3.4 · §8.1 · §9 (екран, обраний на старті) · §11.
5. kernel/wsd/Work_Standard.md — точково: 1.9 · 1.17 · 1.18 · 2.4.

⚠ В-24: судимо ЛИШЕ ПК, CSS-ширини 1920 · 1536 · 1280.
⚠ В-31: нічого не пушимо і не показуємо Олі до кінця всіх екранів.
⚠ В-32: гейт = 7 перевірок продукту (S44 §2) + device. Нових файлів смоуків НЕ заводити.
⚠ П14 · П22: елемент поза макетом або дія оператора — спершу «чи потрібно».
⚠ П23: перед планом екрана — греп id-правил макета + ряди чіпів проти В-21/В-25.
⚠ В-37: Х23.4 (скрол лише списку пропозицій E2) — у ПЕРШОМУ ході будови, окремим рядком гейта.

ПІДГОТОВКА (перший хід, коротко): Lens_validate.py з raw · jsdom (NODE_PATH) · дерево за S44 §5
(AE_WORK_index.html, md5 ee81b79f…) · гейт §2.

── ЗАВДАННЯ СЕСІЇ ──
Вибір наступного екрана (П22: перепитати) — кандидати E3 «Підсумок зміни» (зображення немає, §3.4) · E4.
Хід 1 — план екрана + МІКРОСКОП ПЛАНУ → підтвердження → будова (з Х23.4) → гейт → device.
Кінець чату: нова AE_WORK_index.html у накладній.
Звіт кожного ходу — першим рядком «що ти побачиш» (П6).
```
