# AE-Simulator · самері S38 · «ВІКНО»

живе доки: Х21 (E6 у продукті) не пройшов device-тест
попереднє самері: `AE_Simulator_session_summary_S37_ODYN_ORHAN.md` — **витіснене**.
Усе живе з нього (вироки В-17…В-22) перенесено сюди повним текстом, §0.2.

Робочі файли: `mockup_X21_v1.html` (макет, не змінювався) · `index.html` продукту
(робоче дерево `/mnt/user-data/outputs/AE/`, у цій сесії **не змінювався**).

---

## §0 ВІДКРИТЕ

Черги продукту немає — відкрите живе тут.

### 0.1 Наступний крок: Х21 · E6 у продукті (план погоджено оператором)

Що дає людині: у превʼю зʼявляється **перший екран нового ПК-дизайну — Каталог
(E6)** з боковою панеллю, на живих даних. Решта екранів поки старі.

**Розвідка (грепом, S38):**
- E6 у макеті живе **всередині `normalShell(...)`** — ПК-оболонка з боковою
  навігацією. У продукті оболонки немає: `render()` — простий перемикач
  `{gate, picker, game, result, settings, edit, escen}` у `#app`. E6 їде разом
  з оболонкою.
- CSS макета **36 730 байт**, продукту **24 076**. У макеті **11 голих тегових
  селекторів** (`body button h1 h2 h3 header html input main p select`) і
  `html,body{width:100%;height:100%;overflow:hidden}`. Влитий як є —
  перефарбує старі екрани й уб'є прокрутку. Спільних імен класів **5**:
  `back · field · tabs · tag · trap`.
- Адаптер `fromLive()` **не потрібен**: продукт уже тримає живі дані в
  `S.ALL` (ключ `c`, поле `cat`) і `S.LABEL`.
- Код E6 у макеті: `renderE6 · renderE6Table · E6_FILTERS · E6_COLS · e6Test ·
  e6Rows · e6CountType · e6Cat · e6Term · e6Sort · e6Page · E6_PAGE=20` + вікно
  сторінок (Х16). Оболонка: `normalShell · sidebarMarkup · icon()` + **SVG-спрайт**
  (`#i-box #i-play #i-file #i-chart #i-search #i-arrow #i-bag`…) — залежність,
  яку легко загубити.
- Viewport: макет `width=device-width, initial-scale=1`; продукт
  `…maximum-scale=1, user-scalable=no, viewport-fit=cover`. Медіа макета:
  1180 · 980 · 900 · 680.

**План:**
1. **CSS під коренем `.v2`.** Переносяться лише правила оболонки й E6. Голі теги →
   `.v2 button`, `.v2 h1`…; `html,body{overflow:hidden}` → `body.v2-on`, клас
   вмикається тільки на екранах нового дизайну. Старі екрани не чіпаються.
2. **Оболонка** — у боковій панелі тільки пункти з живим екраном: Тренування
   (старий Picker) · Каталог. Сценарії / Результати / Кураторський зʼявляться
   разом зі своїми екранами (§11.11).
3. **Вхід** — кнопка «Каталог» на старому Picker. Тимчасовий міст, помирає з
   переносом E1.
4. **E6 у стилі продукту** — `el()`, не `innerHTML`: дані Олі йдуть текстом.
   Поведінка за вироками §0.2: без «З бонусом» (В-17); категорії — селектор з
   лічильниками, чіпи типу з переносом (В-20/21); `b+small`, 16px (В-22);
   сторінки по 20; `—` для ціни; бонус числом, порожні в кінці; легенда; жодної
   правки.
5. **Гейти:** `smoke_bundle_v1_2` на новому превʼю · смоуки продукту без регресу
   · новий смоук E6 на превʼю (твердження X16/X17 про E6, перенесені з макета, +
   інʼєкції) — перший факт для кроку 8.

**Мікроскоп плану (1.9):**
| вісь | результат |
|---|---|
| 1 числа | 36 730 / 24 076 · 11 · 5 · 23/181 — прогоном. Очікування нового смоуку не названі — заміряти. |
| 2 детектор | **Дефект, врахований у плані:** «старі екрани не зачеплені» не доводиться `smoke_ui ✓112` — jsdom не рахує стилів. Потрібне статичне твердження: кожен селектор перенесеного блоку починається з `.v2` або `body.v2-on`; у розмітці v2 немає 5 спільних імен. |
| 3 покриття | Канал — греп за ознакою `e6/E6` + обхід викликаних функцій; SVG-спрайт названо. Повноту доводить прогін у jsdom (відсутня функція = помилка). |
| 4 залежності | Макет не чіпається → X14–X17 незмінні. Нових полів даних немає → `ae_rules` / гейт на пуші не зачеплені. `§9 E6` застарів проти В-17/В-20 — переносити за вироками. `config.build` піднімати в момент пушу. |
| 5 маршрут | Код — `index.html` (`outputs/AE`); смоук E6 — Project до кроку 8; превʼю — похідне; пуш у репо — окреме рішення після device-вироку. |

⚠ **Ризик для Х21, названий наперед:** у макеті `meta color-scheme: light`, у
продукту — ні. Переносити разом з E6 (див. §4 П2).

### 0.2 Борг: шість невписаних вироків спеки (з S37 §0.2, повним текстом)

`SPEC §13.3` має бути дописана.

**В-17. Чіп «З бонусом» знято з `§9 E6`.** Грепом по живих даних: позицій без
бонусу — 6; позицій із типом `'—'` — 6; це **та сама шістка** (Бромгексин-8 ·
Фармазолін спрей · Канефрон Н · Ентеросгель паста · Атоксіл гель · Афлубін
краплі). Отже чіп рахував `ВТМ + ЗФ = 175` і дублював перші два чіпи. Ряд типу
міряє **одну вісь**. Бонус лишається сортовною колонкою.

**В-18. `prompts.active` перестало бути полем даних.** `§11.3` забороняє носії з
виглядом даних. Плитка A1 «Промпти» рахує з `promptTexts`: значення = кількість
записів, підпис = кількість **ролей** (усі, крім `characters`) + носій характерів
окремо. Поділ уже жив у `promptNote()`.

**В-19. Порожній підпис плитки не друкується.** `0 чернетки` обіцяє поділ, якого
немає. Підпис із нулем не друкується взагалі.

**В-20. Категорія на E6 — нативний селектор, не чіп-ряд.** 23 категорії в чіпах
давали чотири ряди до першого рядка даних і близнюка «Усі (181) / Усі категорії
(181)». A3 над тим самим каталогом уже фільтрував селектором — розбіжність знято
на користь A3. Селектор стоїть у `.search-row` поруч із пошуком, опції несуть
лічильники.

**В-21. Межа органа фільтра.**
> **До 8 коротких значень — чіпи з переносом. Більше восьми, або довгі
> підписи — нативний селектор поруч із пошуком.** Ряд чіпів **ніколи не
> скролиться**: заховане за жестом дорівнює відсутньому. `.group-filters` дістав
> `flex-wrap:wrap` замість `overflow:auto`.
E1 (7 значень) лишився чіпами, E6 (24) пішов у селектор.

**В-22. Два правила вигляду, які лікують клас, а не адресу.**
- `b + small, strong + small { display:block; }` — дев'ять пар «жирна назва +
  сірий підпис» злипались.
- `input, select, textarea { font-size:16px; }` — iOS зумив сторінку на фокусі.

### 0.3 Питання оператора: чи правильно ляже на різних моніторах

**Відповідь (чесна межа):** розкладку визначає не роздільність матриці, а
ширина в CSS-пікселях після масштабування ОС. Типові: 13″ ноутбук ≈ 1280–1512 ·
Full HD 100 % = 1920, 125 % = 1536 · 2K 125–150 % ≈ 1707–2048 · 2K 100 % = 2560.
Макет гумовий, має стелі ширини (1140–1600) і медіа 1180/980/900/680 — тобто
**задуманий** під діапазон. **Перевірений він не був жодного разу**: jsdom
розкладки не рахує, пристрій оператора — телефон.

**Рішення (делеговано):** у Х21 додати **гейт ширин** — знімки справжнім
рушієм (headless Chromium / Playwright у пісочниці, наявність промацати першим
кроком) на **1280 · 1440 · 1536 · 1920 · 2560**; вирок оком по колажу. Найризиковіші
зони: 1280–1440 (тісно) і ≥1920 (порожнеча). Кандидат на потім: перемикач ширини
рамки превʼю (1280/1440/1920), щоб оператор бачив ширини сам із телефона.

### 0.4 Device-вирок фону превʼю — чекає

`AE_preview_X20_2_v2.html`: фон фарбується шаром `#ae-bg` і рамкою кольором
`--paper`. Це **гіпотеза 2** (див. §1). Якщо фон і далі чорний — переглядач
перебиває не лише `html/body`; наступна проба — фон на внутрішньому контейнері.

### 0.5 Пропозиція в wsd — без відповіді оператора

П3 (§4) — запропоновано, не прийнято. Спитати.

---

## §1 Що зроблено · S38

| хід | предмет | device |
|---|---|---|
| Х20 | `ae_bundle_v1.py` — генератор превʼю: вшиває `ae_rules.js`, прокладка = знімок 8 файлів + підміна `fetch`, решта мережі відхиляється названою помилкою (превʼю **не може** опублікувати правку через `editEndpoint`); сховище в памʼяті, якщо `localStorage` кидає (непрозорий `file://`) | ✓ відкрилось, 43 сценарії |
| Х20.1 | `--desktop` через `meta viewport width=1280` | **✗ — переглядач файлів Claude ігнорує viewport** |
| Х20.2 | `--desktop` = **рамка**: `iframe srcdoc` 1280 + `scale` під ширину екрана; `color-scheme: light` у прокладці | ✓ ПК-сітка 3 колонки · **фон чорний** |
| Х20.2 v2 | фон — шар `#ae-bg` + рамка кольором `--paper` з `index.html` | ⏳ §0.4 |

Оператор після Х20: «я не знаю, що ми зробили і навіщо». Причина — звіт без
першого рядка «що ти побачиш». Урок записано як П6 (§4).

---

## §2 Стан гейтів · прогоном 10.09.2026

**Превʼю** `AE_preview_X20_2_v2.html`:
- `node smoke_bundle_v1_2.js <превʼю>` → **✓29 · ✗0**; `--inject` → **усі 8**,
  набори попарно різні (I1 ✗7 · I2 ✗3 · I3 ✗4 · I4 ✗1 · I5 ✗1 · I6 ✗1 · I7 ✗1 · I8 ✗1);
- без рамки (`--desktop` не дано) → ✓24, інʼєкцій 6 (I6/I8 пропускаються чесно);
- `Lens_validate --html` на рамці → **✓8 · ⚠2 · ✗0**.

**Макет** `mockup_X21_v1.html` (не мінявся): ✓9·⚠1·✗0 · 53/0 · X14 ✓30 · X15 ✓51 ·
X16 ✓54 · X17 ✓40. Червоні станом: X4 ✗14 · X5 ✗1 · X6 ✗4 · regress ✗2.
Падають на `MOCK_DATA`: X8 · X9 · X11 · X12 · X13; X7 окремо.

**Продукт** (`main`, робоче дерево `outputs/AE`): `smoke_ui_v1` ✓112 ·
`smoke_edit_ui_v1` ✓61 · `smoke_step1` ✓28 · `smoke_step2` ✓73 · `smoke_rules_v1`
✓18 · `ae_validate` ✓19·⚠1. **Не заміряні:** `smoke_edit_v2.mjs`, `smoke_step3.js`
— не розібрано виклик; поломкою не називати.
⚠ `BASE` у смоуках продукту зашитий `/mnt/user-data/outputs/AE` — робоче дерево
класти саме туди.
⚠ `mockup_dead_v1.py` на `index.html` → **0/0**: продукт будує DOM у JS. Для
кроку 7 гейт мертвих кнопок — `smoke_ui`, не цей детектор.

---

## §3 Знахідки розвідки

- **Список репо — codeload zip** (`codeload.github.com/Konst-Andre/AE-Simulator/zip/refs/heads/main`,
  розпаковувати `python3 zipfile`). API давав 403 на ліміті — не втрата.
- `index.html` тягне по мережі рівно **8** файлів `fetch('…')`-літералами в `boot()`
  + `<script src="tools/ae_rules.js">`. Динамічні: модель, `ghRead` (GitHub API),
  **POST `editEndpoint` — пише `data/*.json` у репо**.
- Прецедент підміни вже жив у `tools/smoke_ui_v1.js:21–70` — генератор повторив його.
- `?mock=1` у продукті — заглушка моделі (E2 без токенів). У превʼю не ввімкнено:
  вимагало б правки коду в похідному. Вирішувати на E2.
- Архів самері: `archive/summary/` у репо, **33 файли S3…S36 без S27**. Оператор
  розклав і прибрав із Project сам. S1/S2/S27, можливо, на телефоні — не критично.
- У репо лежать `mockup.html` (90 КБ, старий макет) і `mockup UI.zip` (13 МБ) —
  предмет прополки кроку 8.

---

## §4 Правила й патерни, що народились у сесії (повним текстом, 14.29)

**П1 · Самоперевірка — станом, не ознакою.** *Дім: патерн →
`Lens_cookbook_delta_running.md`.*
> Перевірка «чи безпечно вшити текст у `<script>`» за ознакою «є `<!--`» дала
> хибний ✗ на коректному `ae_rules.js` (`<!--…-->` в одному рядку, :115; слово
> `<script>` у коментарі, :250). Правильна форма — автомат станів script-data з
> HTML-специфікації: `data →<!--→ escaped →-->→ data`; `escaped →<script→ double
> →</script→ escaped`; `double →-->→ data`; `</script` у data/escaped = передчасне
> закриття; кінець у double = зламаний тег. Код — `script_unsafe()` у
> `ae_bundle_v1_2.py`, сім випадків перевірено.

**П2 · Переглядач файлів у застосунку Claude — не браузер.** *Дім: патерн →
`Lens_cookbook_delta_running.md` (том iOS/PWA).*
> 1. `meta viewport` ігнорується → ПК-вигляд лише через **рамку**:
>    `iframe srcdoc` фіксованої ширини + `transform:scale(innerWidth/W)`,
>    висота = `innerHeight/s`. У рамки власний viewport, медіа рахуються від W.
> 2. Фон `html/body` перебивається в **обох** документах (зовнішньому й у рамці).
>    Фарбувати власний шар і сам `iframe` (гіпотеза 2, device чекає).
> 3. У `srcdoc` екранувати `& " < >` — інакше вкладені `<script>` стоять у файлі
>    відкритим текстом, і валідатор бере їх за скрипти рамки (✗3).
> 4. jsdom **не вантажить** `iframe srcdoc`: вміст рамки розпаковувати й судити
>    окремо; скрипт масштабу проганяти на самій рамці.

**П3 · Похідне перевіряється реконструкцією до дому.** *Дім: протокол → wsd
(пропозиція, оператор не відповів — §0.5).*
> Шапка «не редагувати» каже, а не ловить. Похідний файл перевіряється тим, що з
> нього знімається все, що додав генератор (маркери `AE-BUNDLE:*`), і результат
> дорівнює дому **байт-у-байт** (A8 у `smoke_bundle`). Застосовно до шаблону
> QR Lens, що регенерується з батчу.

**П4 · Смоук під інʼєкцією читає очікування з файлу, не зі стану сторінки.**
*Дім: протокол → `wsd_delta_running.md`, до S36 §2.2.*
> Під I1 boot падає, `S.cfg = null`, і твердження, що брало `S.cfg.editEndpoint`,
> **обвалило сам смоук** — сховавши решту тверджень. Адреси й числа для тверджень
> брати з файлів у теці, а не з рантайму, який інʼєкція якраз ламає.

**П5 · Рядок `meta charset` — у перших 1024 байтах.** *Дім: патерн → cookbook-дельта.*
> Шапка з 10 хешами sha256 перед `meta charset` виштовхнула його за межу
> розпізнавання кодування. Шапку ставити **після** `meta charset`; коментар
> **до** `<!doctype>` вмикає quirks-режим. Обидва — самоперевірки генератора.

**П6 · Звіт починається з «що ти побачиш».** *Дім: протокол → `wsd_delta_running.md`.*
> Перший превʼю навмисно показував **старий** дизайн (жоден екран ще не
> перенесено) — і звіт цього не сказав. Оператор вирішив, що робота порожня.
> Звіт ходу, результат якого людина дивиться на пристрої, відкривається рядком:
> що саме зʼявиться на екрані і чого там **ще не буде**.

---

## §5 Файли

| файл | куди | стан |
|---|---|---|
| `ae_bundle_v1_2.py` | Project (вручну) до кроку 8 | живий |
| `smoke_bundle_v1_2.js` | Project (вручну) до кроку 8 | живий |
| `AE_Simulator_session_summary_S38_VIKNO.md` | Project | живе самері |
| `AE_Simulator_session_summary_S37_ODYN_ORHAN.md` | архів репо (`archive/summary/`) | витіснене |
| `AE_preview_X20_2_v2.html` | нікуди — похідне, живе до device-тесту | одноразове |

Робоче дерево `outputs/AE/` не переноситься: у новій сесії розкладається з zip
`main` (29 файлів, без `archive/` і `mockup UI.zip`).

---

## §6 Стартове повідомлення наступної сесії

```
Мова спілкування — українська.

СТАРТ (wsd 1.1) — точково:
1. kernel/Lens_INDEX.md — маршрутизатор.
   ⚠ AE-Simulator у §5 НЕМАЄ і не буде — РІШЕННЯ, не борг.
2. AE_Simulator_session_summary_S38_VIKNO.md — живе самері, §0 зверху.
   S37 витіснене й не читається: його вироки В-17…В-22 — у S38 §0.2.
3. AE_Simulator_SPEC_v0_4.md — §9 E6 · §11 · §13.5. v0_3/v0_2 НЕ читати.
4. kernel/wsd/Work_Standard.md — точково за індексом.

⚠ ГРЕП ПЕРЕД ТВЕРДЖЕННЯМ (12.1) — і для очікувань гейта: число з прогону.

ПІДГОТОВКА (перший хід, до плану):
· Lens_validate.py з raw: kernel/Lens_validate.py
· npm install jsdom
· продукт: zip main → /mnt/user-data/outputs/AE (python3 zipfile,
  без archive/ і «mockup UI.zip»). Смоуки продукту чекають саме цей шлях.
· промацати headless Chromium / Playwright — для гейта ширин (S38 §0.3).

ГЕЙТИ:
· превʼю: python3 ae_bundle_v1_2.py -o AE_preview_X<хід>_v<n>.html --desktop
          node smoke_bundle_v1_2.js <превʼю> → ✓29 ; --inject → усі 8
          Lens_validate --html <превʼю> → ✓8 · ⚠2 · ✗0
· продукт (outputs/AE): smoke_ui ✓112 · edit_ui ✓61 · step1 ✓28 · step2 ✓73
          · rules ✓18 · ae_validate ✓19·⚠1
· макет mockup_X21_v1.html: X14 ✓30 · X15 ✓51 · X16 ✓54 · X17 ✓40

── ЗАВДАННЯ СЕСІЇ ──
Х21 — E6 у продукті. План і мікроскоп погоджені: S38 §0.1.
Спершу device-вирок фону превʼю (S38 §0.4), якщо оператор не дав його.
Звіт кожного ходу — першим рядком «що ти побачиш» (S38 §4 П6).

Довіряю, делегую партнере.
```
