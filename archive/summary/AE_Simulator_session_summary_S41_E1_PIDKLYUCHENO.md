# AE-Simulator · самері S41 · «E1 ПІДКЛЮЧЕНО»

живе доки: Х22 (E1 у продукті + двері куратора) не пройшов device-тест
**витісняє S40** (`AE_Simulator_session_summary_S40_E1_VITRYNA.md`): його §0.1 закрито тут, решта
відкритого (§0.2–0.4) і правила П10–П13 перенесені сюди **повним текстом**. S40 → `archive/summary/`.
сусіднє живе самері: **S39** (`…S39_E6_U_PRODUKTI.md`) — його §0.1 · §0.3 (В-17…В-24) · §0.4 · §0.5 і
П1–П9 чинні там і сюди не переписуються. Стеля 2 на продукт: S39 + S41.

Робоче дерево (у репо **не запушене**, відтворюється з `main` — §5):
`main` → `x21_apply_v3` → `x22_apply_v6` → `x22_smokes_apply_v1`. `index.html` md5 `f990d2f5…`.

---

## §0 ВІДКРИТЕ

### 0.1 Наступний крок: Х22.4 — двері в кураторський режим

Зараз низ панелі — одна кнопка «‹ Кураторський режим» (форма й підпис макета), веде в старий
Settings **без замка**. Вирок оператора S41: кнопка має виглядати як двері, а вхід — **під кодом запису**.

**Код — `AE_EDIT_CODE`, не `curatorCode`** (вирок оператора S41). `curatorCode` лежить у
`config.json` відкрито. `AE_EDIT_CODE` — секрет воркера, сторінка його не знає, тому питає воркер:
`POST editEndpoint`, заголовок `x-ae-edit-code`, тіло `{}`. Грепом `worker/ae-edit.js` :223–234:
код перевіряється **до** читання тіла → **403** = не той код; **400** «немає catalog/scenarios» =
код прийнято, нічого не записано. Успіх → `CURATOR.open = true`, `EDITKEY.code = код` (публікація
не питає вдруге; живе до закриття вкладки), екран — старий Settings. Без мережі — «Не вдалося
перевірити код», двері зачинені. **Превʼю** (мережа закрита прокладкою): будь-який непорожній код
+ напис «превʼю: код не перевіряється» — як у макеті (SPEC «Замок», §7.1).

**Вигляд — спершу дешевий compare, потім порт** (вибір асистента, делеговано S41).
Питання чисто візуальне, тож харнес із продуктом не потрібен: один `AE_door_compare_v1.html`,
CSS — копія блоку `v2-css` (ті самі токени), панель 220 px (на телефоні вміщується без рамки).
Три варіанти × три стани (зачинено · розкрито з полем · «Код не підходить»):
- **А** — картка-дзеркало бренду: плитка з замком (геометрія `.brand-mark`), два рядки
  «Кураторський режим / відкривається кодом», шеврон. *Рекомендація асистента.*
- **Б** — перемикач «Навчання | Куратор». Ризик §11.11: кураторської панелі v2 ще немає — пів
  перемикача мертве.
- **В** — тихий рядок із роздільником і замком (найближче до теперішнього; оператор його відкинув).
Розкриття — **на місці**, без модалки (SPEC: «лишається де був»). Оператор обирає оком → порт.

Порт (після вибору): код дверей — у блоці v2 (`sidebar()`), CSS — у `v2-css-own` (П13), символ
`i-lock` — власний (у макеті немає; §11.5 — монохромний). Смоуки: `smoke_X21` S17 (двері, три
відповіді воркера мокнутим fetch) + твердження **з файлу воркера** (П4): перевірка коду стоїть до
`JSON.parse` — інакше «400 = код прийнято» бреше; `edit_ui` `openEditor` — через двері
(fetch у ньому вже мокнутий). px — варіант проти себе ж у compare (макета для дверей немає).
⚠ Після дверей стара панель «Код куратора» в Settings стає другими, слабшими дверима за першими —
лишити чи прибрати, вирішити в плані Х22.4.

### 0.2 Спека — дописати (борг росте)

- `SPEC §13.3`: **В-25** (текст — нижче) разом із боргом S39 §0.3 (В-17…В-24).
  > **Ряд чіпів допустимий, лише коли на найвужчій ширині гейта (1280) вміщується в ОДИН
  > ряд. Інакше — нативний селектор: поруч із пошуком, коли колонка широка (E6), або під
  > пошуком на всю ширину, коли колонка вузька (E1). Кількість значень — не критерій,
  > критерій — ширина.** В-21 уточнюється, не скасовується.
- `§9 E1`: «Фільтр по `grp` — селектор з лічильниками, фасетними в межах пошуку».
- **В-26 (S41).** Екран імені знято з продукту — виконання §2.3 і §11.7. Ім'я ніде не
  використовувалось, крім показу (грепом: воркер його не бачить).
- **В-27 (S41).** Низ панелі звичайного режиму — лише вхід «Кураторський режим» (як макет, §8.1).
  Окремих «Налаштувань» і «Змінити ім'я» немає: зміст налаштувань (ключ, модель, редактор) —
  кураторський, його доми A6 і A2.
- **В-28 (S41).** `§9 «Замок»`: двері відмикає **код запису** `AE_EDIT_CODE`, перевіряє воркер.
  Рядок спеки «код зберігається в налаштуваннях і той самий код відмикає запис» — неправда вже
  зараз (ключів два, `index.html` :1490–1500) — переписати.

### 0.3 Пуш (за оператором)

Їде: `index.html` = ланцюг §5 · 4 смоуки `tools/` (`x22_smokes_apply_v1`). `smoke_edit_ui` крутиться
в CI (`perevirka.yml`) — пуш без нього зламає CI. У момент пушу: підняти `config.build` і
дописати `ДЛЯ_ОЛІ/ЖУРНАЛ_ЗМІН.md` («що змінилось для неї»: новий екран «Тренування» — список і
довідка, група вибирається списком; **екрана з іменем більше немає**, тренажер відкривається
одразу на списку; налаштування — кнопкою «Кураторський режим» унизу панелі). Розбіжність
журналу `v10` проти збірки `v30` (S39 §0.4) — **досі не розібрана**. Раджу пушити **після**
дверей (Х22.4): інакше налаштування з ключем стоять відчинені.

### 0.4 Кандидат інструмента (не зроблено)
`ae_bundle --width 1920|1536|1280` — рамка превʼю досі 1280. Не блокує.

### 0.5 Виправлення запису S40
S40 §0.1 назвав два мости «Налаштування» / «Змінити ім'я» **вироком оператора**. Звірено з
чатом S40: оператор обрав **мою рекомендацію** на питання «куди їх подіти», а питання «чи вони
потрібні» я не поставив і розбіжність з макетом не назвав. Скасовано вироком S41 (В-27). → П14.

---

## §1 Що зроблено · S41

| хід | предмет | чим доведено | device |
|---|---|---|---|
| — | підготовка: validator з raw, jsdom, Chromium 141; дерево з `main` | усі гейти = S40 §2 | — |
| Х22.3б | низ панелі + банер (два мости) — **відкинуто** оператором | — | — |
| Х22.3б′ | низ панелі = макет: одна кнопка «Кураторський режим» + `i-arrow` (спрайт з макета) | px B: E1 = макет **без** патча низу; A: E6 і старі екрани незмінні | ⏳ |
| Х22.3в | екран імені знято: стан, старт, диспетчер, «назад», `Gate`, привітання й «Змінити ім'я» в запасному Picker, рядок «ім'я · аптека» на E3 — 8 правок одним списком `x22_old_edits_v1.json` | S5c/S5b відкочують список і звіряють з `main`; I26 (неназвана правка) → ✗ | ⏳ |
| Х22.3г | `V2.picker = Training`; старий Picker — запасний до кроку 8 (Р1); міграція 4 смоуків | нові нормативи §2 з прогону | ⏳ демо `AE_preview_X22_3g_v1.html` |

Рішення оператора S41: Р1 — Picker запасний до кроку 8 · Р2 — на телефоні (<980) низ панелі
схований портованим CSS, як у макеті: прийнято (В-24) · банер «Модель не підключена» — прибрано
(у макеті немає, а умова читає оголошення `workerReady:true` — ніколи не з'явився б і мертвий
воркер не спіймав би) · екран імені — прибрати разом із Х22.3.

---

## §2 Стан гейтів · прогоном 10.09.2026 (дерево §5)

- **Х21:** `node smoke_X21_v9.js <AE/index.html> --base <index.html з main>` → **✓36 · ✗0**; `--inject` → **усі 24**.
- **Продукт:** smoke_ui **✓112** · edit_ui **✓61** · step1 **✓26** · step2 **✓73** · rules ✓18 · ae_validate ✓19·⚠1.
  (step1 28 → 26: «перейшли до списку» і «імʼя збережено» мертві; smoke_ui: «імʼя не постраждало»
  мертве, «старт одразу на E1» нове; марка збірки переїхала з екрана входу в кураторський екран.)
- **Рушій:** `python3 px_x22_3b_v3.py --self` → ✗0; без прапорця → **✗0** (A: x21↔x22 settings·game·catalog,
  main↔x22 settings·game; B: E1 проти макета 7 станів × 1280/1536/1920); `--inject` → ✗21.
  Чекає в `/home/claude/`: `b_main · b_x21 · b_x22` (ae_bundle без `--desktop`) + `mock.html`.
- **Превʼю** `AE_preview_X22_3g_v1.html`: `smoke_bundle_v1_3` **✓31** · inject 8/8 · Lens_validate ✓8·⚠2·✗0.
- **Макет:** X14 ✓30 · X15 ✓51 · X16 ✓54 · X17 ✓40.
- Смоукам продукту — `NODE_PATH` на `node_modules`.

---

## §3 Знахідки

- Зникнення екрана імені приспало I5 у `smoke_bundle`: сховище ловилось лише тому, що старт
  читав ім'я. v1_3 додав B5 — ключ моделі пишеться й читається. → П17.
- Перша I22 («міст веде в edit») не спрацювала: `Editor` без замка сам повертає в `settings`. → П16.
- `px_x22_1` і `px_x22_3a` на новому файлі дали б хибний ✗ — замінені одним `px_x22_3b_v3`.
- Ім'я друкувалось у трьох місцях (Gate, привітання Picker, E3); воркер його не отримує.

---

## §4 Правила й патерни (повним текстом, 14.29)

**Перенесені з S40 (не вписані):**

**П10 · Двійники класів.** *Дім: патерн → `Lens_cookbook_delta_running.md` (поруч із П9).*
> Корінь `.v2` захищає старе від нового, але **не нове від старого без кореня**: старе
> `.tag{margin-left:6px}` протече в новий екран. Тому при переносі класи, що збігаються з
> іменами старого CSS, перейменовуються **явною мапою** в генераторі (`RENAME`), а генератор
> **падає** на будь-якому зіткненні, крім названих мостів. Класи рахуються без коментарів.

**П11 · Піксельна звірка — лише на детермінованому растрі.** *Дім: патерн → cookbook-дельта (до П8).*
> Headless Chromium з GPU-растром дає ±1 кольору на згладжуванні заокруглених кутів між
> прогонами **того самого файла** (S40: 8 з 18 знімків червоні). Запуск:
> `--disable-gpu --disable-gpu-rasterization --disable-partial-raster --force-color-profile=srgb --disable-lcd-text`,
> кожен стан — зі свіжого завантаження з накопиченими кроками, `animations='disabled'`.
> Перед вироком — **два контролі**: «файл проти себе» = 0 ✗ і інʼєкція (1 px) = ✗.

**П12 · Побудова ≠ підключення.** *Дім: протокол → `wsd_delta_running.md`.*
> Екран спершу будується **непідключеним** (функція є, маршрут на неї — ні) і доводиться
> звіркою: гейти продукту не рухаються. Підключення — окремий крок, і **смоуки старого
> екрана мігрують тим самим кроком**: кожне їхнє твердження або переписане, або назване
> мертвим. Інакше підключення валить гейти, і червоне змішується з регресом.

**П13 · Власні правила v2 — окремим блоком `v2-css-own`.** *Дім: патерн → cookbook-дельта.*
> Правило, якого немає в макеті (свідоме відхилення — В-25), не дописується в генерований
> блок (його S4 звіряє з портом байт-у-байт). Воно живе в `<style id="v2-css-own">` одразу
> після `v2-css`, пишеться руками з шапкою «живе доки», селектори з `.v2`. Префікс `v2-css`
> в id — умова гарда зіткнень порту. У П8 те саме правило входить у патч макета (П-г).
> Коли макет отримає це відхилення — правило переїжджає в порт.

**Нові · S41:**

**П14 · Питання оператору — «чи потрібно», а не «куди».** *Дім: протокол → `wsd_delta_running.md`.*
> Пропозиція, що додає на екран елемент, якого немає в макеті, спершу звіряється з макетом і
> спекою, і розбіжність **називається в самому питанні**. Питання «куди подіти X» без «чи
> потрібен X» підсовує відповідь. Рядок «вирок оператора» в самері пишеться лише тоді, коли
> оператор сформулював рішення сам; якщо обрав рекомендацію — так і писати: «обрав рекомендацію
> асистента». *Анти-приклад S40→S41: два мости внизу панелі, записані як вирок, — хід роботи і
> звірки викинуто.*

**П15 · Правки старого коду — названим списком з одним домом.** *Дім: патерн → cookbook-дельта (поруч із П3).*
> Коли перенос мусить змінити код поза шаром v2 (екран імені), правки лежать списком
> `[назва, було, стало]` в одному файлі. Генератор застосовує їх («рівно один збіг»), звірка з
> `main` **відкочує** той самий список і лише потім порівнює. Неназвана правка поруч лишається
> червоною (I26). Два списки — у генераторі й у смоуку — розійшлися б мовчки.

**П16 · Інʼєкцію може поглинути захист продукту.** *Дім: протокол → `wsd_delta_running.md` (кандидат у 1.15).*
> Інʼєкція, наслідок якої продукт сам зводить назад до правильного стану (перехід у `edit` без
> замка → `settings`), дає «не спіймано» при чесному детекторі. Перш ніж лагодити детектор —
> перевірити, чи інʼєкція взагалі ламає спостережуване.

**П17 · Детектор, що живе побічним шляхом, засинає разом із ним.** *Дім: протокол → `wsd_delta_running.md` (кандидат у 1.15).*
> I5 ловився не перевіркою сховища, а тим, що старт читав ім'я. Прибрали функцію — детектор
> осліп без жодного червоного. Коли з продукту йде функціональність, прогнати `--inject` усіх
> гейтів і для кожного «не спіймано» знайти, яким шляхом він ловився.

---

## §5 Файли · накладна

**Project — додати:**
| файл | живе доки |
|---|---|
| `AE_Simulator_session_summary_S41_E1_PIDKLYUCHENO.md` | живе самері |
| `x22_apply_v6.py` · `x22_training_v4.js` · `x22_old_edits_v1.json` | пуш Х22 |
| `x22_smokes_apply_v1.py` | пуш Х22 (тоді смоуки в репо) |
| `smoke_X21_v9.js` | крок 8 |
| `smoke_bundle_v1_3.js` | пуш Х22 |
| `px_x22_3b_v3.py` | device Х22 |

**Project — видалити (замінені цією сесією):** `x22_apply_v3.py` · `x22_training_v2.js` ·
`smoke_X21_v6.js` · `smoke_bundle_v1_2.js` · `px_x22_1.py` · `px_x22_3a.py`.
**Спершу в архів репо (`AE-Simulator/archive/summary/`), потім видалити:** S40 (у репо немає).
**Список S40, досі не виконаний:** видалити S35 · S36 (байт-у-байт в архіві) · `QR_Lens_…B59…` ·
`Lens_…governance_E.md`; S37 · S38 — спершу в архів; кандидати `AE_Simulator.md` · `README_AE_Simulator.md`.
**Не чіпати:** `x21_apply_v3.py` · `e6_probe_v1.js` · `v2css_port_v1.py` (ланцюг Х21) · `v2_probe_v2.js` ·
`v2css_port_v2.py` · `x22_own_v1.css` · `mockup_X21_v1.html` · `ae_bundle_v1_2.py` · X14–X17 · X4…X13 (крок 8).

**Смоуки продукту:** мертві твердження вже прибрані й названі коментарем у самих файлах
(`smoke_ui` — «ім'я не постраждало»; `step1` — «перейшли до списку», «імʼя збережено»). Більше
прибирати нічого — решта переписана на E1. `AE_preview_X22_3g_v1.html` — похідне, нікуди.

**Робоче дерево в новій сесії** (усі інструменти + `mockup_X21_v1.html` в одній теці, `NODE_PATH`):
zip `main` → `outputs/AE` (29 файлів, без `archive/` і `mockup UI.zip`), далі
`node e6_probe_v1.js && python3 x21_apply_v3.py AE/index.html && node v2_probe_v2.js && python3 x22_apply_v6.py AE/index.html && python3 x22_smokes_apply_v1.py AE`.
Перевірка: md5 `index.html` = `f990d2f5eb5d0dce23667d7509dffd39`; `smoke_X21_v9` ✓36.
Повтор `x22_apply_v6` і `x22_smokes_apply_v1` нічого не змінює (звірено); `x21_apply_v3` поверх
Х22 падає на гарді спрайту — це запобіжник.

---

## §6 Стартове повідомлення наступної сесії

```
Мова спілкування — українська.

СТАРТ (wsd 1.1) — точково:
1. kernel/Lens_INDEX.md — маршрутизатор.
   ⚠ AE-Simulator у §5 НЕМАЄ і не буде — РІШЕННЯ, не борг.
2. AE_Simulator_session_summary_S41_E1_PIDKLYUCHENO.md — живе самері, §0 зверху (S40 витіснене).
3. AE_Simulator_session_summary_S39_E6_U_PRODUKTI.md — лише §0.3 · §4 (П1–П9).
4. AE_Simulator_SPEC_v0_4.md — §8.1 · §9 «Замок кураторського режиму» · §11.
5. kernel/wsd/Work_Standard.md — точково: 1.9 · 1.17 · 2.4.

⚠ В-24: судимо ЛИШЕ ПК, CSS-ширини 1920 · 1536 · 1280. Телефон — перегляд.
⚠ П14: елемент поза макетом — спершу звірити з макетом і назвати розбіжність у питанні.
⚠ ГРЕП ПЕРЕД ТВЕРДЖЕННЯМ (12.1) — і для очікувань гейта: число з прогону.

ПІДГОТОВКА (перший хід):
· Lens_validate.py з raw: kernel/Lens_validate.py
· npm install jsdom (NODE_PATH) ; Playwright+Chromium — промацати
· дерево: S41 §5 (ланцюг до x22_smokes_apply_v1) → md5 f990d2f5…
· бандли для рушія: b_main · b_x21 · b_x22 (ae_bundle без --desktop) + mock.html

ГЕЙТИ (S41 §2):
· Х21: smoke_X21_v9 ✓36 ; --inject усі 24
· продукт: smoke_ui ✓112 · edit_ui ✓61 · step1 ✓26 · step2 ✓73 · rules ✓18 · ae_validate ✓19·⚠1
· рушій: px_x22_3b_v3 --self ✗0 → ✗0 ; --inject ✗>0
· превʼю: smoke_bundle_v1_3 ✓31 · inject 8 ; Lens_validate ✓8·⚠2·✗0
· макет: X14 ✓30 · X15 ✓51 · X16 ✓54 · X17 ✓40

── ЗАВДАННЯ СЕСІЇ ──
Х22.4 — двері в кураторський режим (S41 §0.1).
Перший хід: підготовка + AE_door_compare_v1.html (А · Б · В × зачинено · розкрито · «не підходить»)
— одразу, без окремого ходу плану: вибір дешевого compare делеговано в S41. Далі мій вибір оком
→ план порту з МІКРОСКОПОМ → порт (код запису AE_EDIT_CODE через воркер, превʼю — будь-який код).
Звіт кожного ходу — першим рядком «що ти побачиш» (П6).

Довіряю, делегую партнере.
```
