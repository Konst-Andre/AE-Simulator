# x23_smokes_apply_v1.py — Х23.3: міграція смоуків продукту під E2 v2 (П12: тим самим кроком, що й підключення)
# живе доки: фінальний пуш (В-31) — рецепт дерева S44 §5, після x22_4_smokes_apply_v2.py
# запуск: python3 x23_smokes_apply_v1.py AE    · повтор — без змін (кожне «стало» вже стоїть рівно раз)
# Пари [файл, назва, було, стало] — один дім (П15). Нових файлів смоуків немає (В-32).
import sys, os
root = sys.argv[1] if len(sys.argv) > 1 else 'AE'
PAIRS = [('smoke_ui_v1.js', 'інʼєкція 2: суддя → Talk', "html=html.replace('if(engine.judge){','if(false){');", 'html=html.replace("if(S.ended && typeof engine.judge === \'function\'){",\'if(false){\'); /* Х23.3: ціль — Talk */'), ('smoke_ui_v1.js', 'інʼєкція 3: Настрій → Talk', 'html=html.replace("el(\'dt\',{text:\'Настрій\'}), el(\'dd\',{text:sc.mood}),", \'\');', 'html=html.replace(", [\'Настрій\', sc.mood]]", \']\'); /* Х23.3: ціль — брифінг Talk */'), ('smoke_ui_v1.js', 'інʼєкція 4: згортання (мертве) → ціль у картці', 'html=html.replace("foldRows(briefDl,[\'Настрій\',\'Ціль\']);", \'\');', '/* Х23.3: згортання на E2 немає (рішення S44, відхилення від SPEC §3.2) — інʼєкція переведена на ціль:\n     рядок «Ваше завдання» на місці, але показує заглушку замість цілі сценарію. */\n  html=html.replace("el(\'p\', {text:sc.goal || \'Коректно опрацювати запит клієнта.\'})", "el(\'p\', {text:\'Коректно опрацювати запит клієнта.\'})");'), ('smoke_ui_v1.js', 'шапка · брифінг · діалог · Р1–Р3 · розмова до кінця → E2 v2', "  console.log('\\n— шапка й панель розмови —');\n  d.querySelector('[data-start-one]').click();\n  T('шапка — дві сходинки', !!d.querySelector('.top .toprow') && !!d.querySelector('.top .toptitle'));\n  T('заголовок сценарію в своєму рядку',\n    d.querySelector('.toptitle').textContent===S.sc.title);\n  /* Брифінг. Читаємо живу DOM: пара dt/dd, значення — з того самого\n     сценарію, який зараз грається. Без цього твердження втрата рядка\n     проходить тихо — так і сталось із mood у S5. */\n  const dts=[...d.querySelectorAll('.brief dt')].map(x=>x.textContent);\n  const dds=[...d.querySelectorAll('.brief dd')].map(x=>x.textContent);\n  T('брифінг показує настрій клієнта', dds[dts.indexOf('Настрій')]===S.sc.mood);\n  /* Порядок змінено свідомо (S8): «Настрій» переїхав з-під «Клієнта» у\n     хвіст, до «Цілі». Це два довгі згортані рядки, і вони мусять бути\n     сусідами — інакше керовані шеврони розкидані по таблиці. */\n  T('настрій стоїть поряд із ціллю, у хвості брифінгу',\n    dts.indexOf('Настрій')>=0 &&\n    dts.indexOf('Настрій')+1===dts.indexOf('Ціль') &&\n    dts.indexOf('Ціль')===dts.length-1);\n  /* Згортання. Клас — на dt і dd, стан — атрибутами. Перевіряємо обидва\n     рядки окремо: третя ін'єкція виносить лише «Настрій», і без окремого\n     твердження про «Ціль» зняте згортання пройшло б тихо. */\n  const foldDt=[...d.querySelectorAll('.brief dt.fold')].map(x=>x.textContent.trim());\n  T('настрій згортається', foldDt.includes('Настрій'));\n  T('ціль згортається', foldDt.includes('Ціль'));\n  const goalDd=dds.length?d.querySelectorAll('.brief dd')[dts.indexOf('Ціль')]:null;\n  T('ціль відкрита за замовчуванням',\n    !!goalDd && goalDd.classList.contains('fold-b') && goalDd.getAttribute('data-open')==='1');\n  /* ⚠ Довжина перевіряється окремо: .every на порожньому наборі істинний,\n     тож без неї зникле згортання давало б зелений ✓ (1.15, пастка 4). */\n  const foldEls=[...d.querySelectorAll('.brief dt.fold')];\n  T('шеврон згортання — інлайновий SVG, не гліф',\n    foldEls.length===2 && foldEls.every(x=>!!x.querySelector('svg.chev')));\n\n  const chat=d.querySelector('.chat');\n  T('панель розмови існує', !!chat);\n  T('репліки лежать усередині панелі', !!chat && !!chat.querySelector('.log .turn'));\n  T('поле вводу лежить усередині панелі', !!chat && !!chat.querySelector('.say textarea'));\n  T('кнопка «Сказати» лежить усередині панелі',\n    !!chat && [...chat.querySelectorAll('button')].some(b=>b.textContent.trim()==='Сказати'));\n  T('підказка видима, доки розмови немає',\n    !!chat && !chat.querySelector('.chat-hint').classList.contains('hide'));\n  T('[джерело] фон панелі темніший за сторінку', /--chat:#E7ECE8/.test(src));\n  T('[джерело] крапки «клієнт друкує» є', /\\.dots i\\{/.test(src) && /S\\.waiting/.test(src));\n\n  /* Переповнення по горизонталі. jsdom розкладки не має, тому б'ємо\n     в дві причини, які її створювали: колонку з min-width:auto і\n     дитину, ширшу за батька. */\n  T('[джерело] мобільна колонка має min-width 0',\n    /@media\\(max-width:880px\\)\\{\\.stage\\{grid-template-columns:minmax\\(0,1fr\\)\\}\\}/.test(src));\n  T('[джерело] відʼємних полів усередині панелі немає',\n    !/\\.say\\{[^}]*margin:0 -12px/.test(src) && !!d.querySelector('.chat > .chat-body'));\n  T('[джерело] довгий токен переноситься в бульбашці', /overflow-wrap:anywhere/.test(src));\n\n  console.log('\\n— розмова до кінця (заглушка) —');\n  const ta=d.querySelector('.say textarea');\n  const say=[...d.querySelectorAll('.say button')][0];\n  /* Enter на ПК. Кількість ходів не міняється: перший робиться клавішею,\n     решта — кнопкою. Додати шостий хід означало б тиснути в заглушене\n     поле, і твердження про завершення зеленіло б від чужої причини.\n     ⚠ Shift+Enter перевіряється ПЕРШИМ і на тому самому тексті: якби він\n     надіслав, наступна перевірка Enter'а все одно побачила б репліку в\n     стрічці й нічого не помітила. */\n  const key=(o)=>ta.dispatchEvent(new w.KeyboardEvent('keydown',\n    {key:'Enter',bubbles:true,cancelable:true,...o}));\n  ta.value='репліка 0'; ta.dispatchEvent(new w.Event('input'));\n  key({shiftKey:true}); await new Promise(r=>setTimeout(r,400));\n  T('Shift+Enter не надсилає — це новий рядок', S.history.length===0);\n  key({}); await new Promise(r=>setTimeout(r,400));\n  T('Enter надсилає репліку там, де є миша', S.history.length>0);\n  T('[джерело] на дотиковому екрані Enter лишається новим рядком',\n    /POINTER_FINE\\s*=/.test(src) && /hover:hover\\) and \\(pointer:fine/.test(src) &&\n    /e\\.metaKey \\|\\| e\\.ctrlKey \\|\\| POINTER_FINE/.test(src));\n  for(let i=1;i<5;i++){\n    ta.value='репліка '+i; ta.dispatchEvent(new w.Event('input'));\n    say.click(); await new Promise(r=>setTimeout(r,400));\n  }\n  T('розмову завершено', S.ended===true);\n  T('розбір має власний вигляд, не бульбашку клієнта',\n    !!d.querySelector('.turn.review') && !d.querySelector('.turn.review').classList.contains('client'));\n  T('поле вводу заглушене після завершення', ta.disabled===true);\n  T('кнопка «Сказати» заглушена після завершення', say.disabled===true);\n  T('поле каже, чому воно не працює', ta.placeholder==='Розмову завершено');\n  T('підказку прибрано після завершення',\n    d.querySelector('.chat-hint').classList.contains('hide'));\n\n", "  console.log('\\n— шапка й панель розмови (E2 v2, Х23.3) —');\n  d.querySelector('[data-start-one]').click();\n  /* Х23.3: екран перебудовується на кожну зміну розмови (render) — вузли беремо щоразу заново.\n     Мертві з переносом (S44 §1): дві сходинки .top · згортання «Настрій/Ціль» (4) · порядок хвоста\n     брифінгу · підказка під стрічкою (2) · фон --chat і дві [джерело] мобільної колонки старого екрана. */\n  const TA=()=>d.querySelector('.v2 .composer textarea'), SEND=()=>d.querySelector('.v2 .send');\n  T('шапка: «назад» і заголовок — в appbar', !!d.querySelector('.appbar .v2-back') && !!d.querySelector('.appbar .case-title h1'));\n  T('заголовок сценарію — у h1', d.querySelector('.case-title h1').textContent.startsWith(S.sc.title));\n  T('на E2 панель несе лише бренд — ні навігації, ні дверей (макет)',\n    !!d.querySelector('.v2 .sidebar .brand') && !d.querySelector('.v2 .side-links') && !d.querySelector('.v2 .door'));\n  /* Брифінг «Про клієнта»: значення — з того самого сценарію, що грається (урок mood, S5). */\n  const facts={}; d.querySelectorAll('.profile .fact').forEach(f=>facts[f.querySelector('dt').textContent]=f.querySelector('dd').textContent);\n  T('брифінг показує настрій клієнта', facts['Настрій']===S.sc.mood);\n  T('брифінг показує контекст і характер', facts['Контекст']===(S.sc.mode||'—') && 'Характер' in facts);\n  T('ціль сценарію — у картці «Ваше завдання»', !!S.sc.goal && d.querySelector('.profile .notice p').textContent===S.sc.goal);\n  const chat=d.querySelector('.v2 .dialogue');\n  T('панель розмови існує', !!chat);\n  T('перша репліка клієнта лежить у панелі', !!chat && chat.querySelectorAll('.messages .message').length===1 && chat.querySelector('.bubble').textContent===S.sc.open);\n  T('поле вводу лежить у панелі', !!chat && !!chat.querySelector('.composer textarea'));\n  T('кнопка надсилання лежить у панелі', !!chat && !!chat.querySelector('.composer .send'));\n  T('[джерело] крапки «клієнт друкує» є', /\\.v2 \\.typing i \\{/.test(src) && /S\\.waiting/.test(src));\n  T('[джерело] довгий токен переноситься в бульбашці v2', /\\.v2 \\.bubble \\{ overflow-wrap:anywhere/.test(src));\n\n  console.log('\\n— чек і пропозиції (Р1 · Р2 · §6.4) —');\n  const cartBox=d.querySelector('.cart-panel .panel');\n  T('Р1: у чеку немає гривні — бонусів і сум фармацевт тут не бачить (§9 E2)', !!cartBox && !/₴/.test(cartBox.textContent));\n  T('чек = замовлення', d.querySelectorAll('.cart-row').length===S.sc.order.filter(c=>S.ALL[c]).length);\n  const pool=w.eval('poolOf(S.sc)').map(i=>i.c);\n  T('Р2: дві вкладки, відкрита «Для цієї ситуації» = poolOf',\n    d.querySelectorAll('.v2-tabs .tab').length===2 && d.querySelector('.tab.active').dataset.pool==='case' &&\n    [...d.querySelectorAll('.proposal-row .plus')].every(b=>pool.includes(b.dataset.code)));\n  const cart0=JSON.stringify(S.cart), plus=d.querySelector('.proposal-row .plus'), pname=S.ALL[plus.dataset.code].n;\n  plus.click();\n  T('«+» дописує назву в поле, чек не змінено (§6.4)', TA().value.includes(pname) && JSON.stringify(S.cart)===cart0);\n  TA().value='чернетка'; TA().dispatchEvent(new w.Event('input')); w.render();\n  T('чернетка переживає render()', TA().value==='чернетка');\n  TA().value=''; TA().dispatchEvent(new w.Event('input'));\n  T('кнопок кінця до кінця розмови немає', !d.querySelector('[data-e2-end]'));\n\n  console.log('\\n— розмова до кінця (заглушка) —');\n  /* Enter на ПК. Кількість ходів не міняється: перший — клавішею, решта — кнопкою.\n     ⚠ Shift+Enter перевіряється ПЕРШИМ і на тому самому тексті. */\n  const key=(o)=>TA().dispatchEvent(new w.KeyboardEvent('keydown',\n    {key:'Enter',bubbles:true,cancelable:true,...o}));\n  TA().value='репліка 0'; TA().dispatchEvent(new w.Event('input'));\n  key({shiftKey:true}); await new Promise(r=>setTimeout(r,400));\n  T('Shift+Enter не надсилає — це новий рядок', S.history.length===0);\n  key({}); await new Promise(r=>setTimeout(r,400));\n  T('Enter надсилає репліку там, де є миша', S.history.length>0);\n  T('[джерело] на дотиковому екрані Enter лишається новим рядком',\n    /POINTER_FINE\\s*=/.test(src) && /hover:hover\\) and \\(pointer:fine/.test(src) &&\n    /e\\.ctrlKey \\|\\| e\\.metaKey \\|\\| POINTER_FINE/.test(src));\n  for(let i=1;i<5;i++){\n    TA().value='репліка '+i; TA().dispatchEvent(new w.Event('input'));\n    SEND().click(); await new Promise(r=>setTimeout(r,400));\n  }\n  T('розмову завершено', S.ended===true);\n  T('розбір має власний вигляд, не бульбашку клієнта',\n    !!d.querySelector('.talk-review') && !d.querySelector('.talk-review').closest('.message'));\n  T('поле вводу заглушене після завершення', TA().disabled===true);\n  T('кнопка надсилання заглушена після завершення', SEND().disabled===true);\n  T('поле каже, чому воно не працює', TA().placeholder==='Розмову завершено');\n  T('Р3: «Пройти ще раз» і вихід — у шапці, лише після кінця', d.querySelectorAll('.appbar [data-e2-end]').length===2);\n\n"), ('smoke_ui_v1.js', 'шапка розбору → .talk-review', "/РОЗБІР · приріст/.test(d.querySelector('.review .who').textContent)", "/РОЗБІР · приріст/.test(d.querySelector('.talk-review strong').textContent)"), ('smoke_ui_v1.js', 'суддя: сліпий детектор → .talk-review', "T('розбір прийшов від судді, не від клієнта', /Заглушка судді/.test(d.body.textContent));", "/* Х23.3: було d.body.textContent — там завжди є <script> зі стабом, тож твердження не ловило нічого й ніколи (1.15). */\n  T('розбір прийшов від судді, не від клієнта', /Заглушка судді/.test((d.querySelector('.talk-review')||{}).textContent||''));"), ('smoke_ui_v1.js', 'фраза → .talk-say', "!!d.querySelector('.review .phrase')", "!!d.querySelector('.talk-review .talk-say')"), ('smoke_ui_v1.js', '«ще раз» → v2 + гонка пізньої відповіді', "  retry.click();\n  T('стрічка очистилась до першої репліки', d.querySelectorAll('.log .turn').length===1);\n  T('розмова знову жива', S.ended===false);\n  T('поле вводу знову працює', ta.disabled===false);\n  T('чек повернувся до початкового замовлення',\n    JSON.stringify(S.cart)===JSON.stringify(S.sc.order));\n  T('спроба порахована', S.attempt===2);\n  T('розбір і фраза скинуті', S.feedback==='' && S.phrase==='');\n  T('кнопки кінця сховані', retry.classList.contains('hide'));\n\n  S.screen='picker'; w.render();\n\n", "  retry.click();\n  T('стрічка очистилась до першої репліки', d.querySelectorAll('.v2 .messages .message').length===1);\n  T('розмова знову жива', S.ended===false);\n  T('поле вводу знову працює', TA().disabled===false);\n  T('чек повернувся до початкового замовлення',\n    JSON.stringify(S.cart)===JSON.stringify(S.sc.order));\n  T('спроба порахована', S.attempt===2);\n  T('розбір і фраза скинуті', S.feedback==='' && S.phrase==='');\n  T('кнопки кінця сховані', !d.querySelector('[data-e2-end]'));\n  /* Х23.3: відповідь, що прийшла після виходу, не пише в нову розмову. Пізня — четверта репліка: на ній\n     заглушка завершує, тож перехоплення видно по S.ended (П16: перша дала б той самий чек і ended=false). */\n  for(let i=0;i<3;i++){ TA().value='р '+i; TA().dispatchEvent(new w.Event('input')); SEND().click(); await new Promise(r=>setTimeout(r,400)); }\n  TA().value='і пішов'; TA().dispatchEvent(new w.Event('input')); SEND().click();\n  d.querySelector('.v2-back').click(); d.querySelector('[data-start-one]').click();\n  const fresh=JSON.stringify(S.cart); await new Promise(r=>setTimeout(r,500));\n  T('пізня відповідь не зачепила нову розмову', S.history.length===0 && S.ended===false && JSON.stringify(S.cart)===fresh);\n\n  S.screen='picker'; w.render();\n\n"), ('smoke_ui_v1.js', '«назад» → .v2-back', "  const back=d.querySelector('.back');", "  const back=d.querySelector('.v2-back');"), ('smoke_step1.js', 'сума: зі стрічки → з шапки розбору (Р1) · діалог → v2', "  // ключове: суму читаємо ЗІ СТРІЧКИ, тобто з коду застосунку, а не рахуємо самі\n  const tapeTxt=d.querySelector('.tape').textContent;\n  const expBase=S.sc.order.reduce((a,c)=>a+S.ALL[c].b,0);\n  const shown=(tapeTxt.match(/Було в замовленні([\\d\\s,]+)/)||[])[1];\n  T('стрічка показує правильну суму замовлення ('+expBase.toFixed(2).replace('.',',')+')',\n    !!shown && shown.trim()===expBase.toFixed(2).replace('.',','));\n  T('сума замовлення не нульова', !!shown && parseFloat(shown.replace(',','.'))>0);\n  const ta=d.querySelector('.say textarea');\n  const say=async t=>{ ta.value=t; ta.dispatchEvent(new w.Event('input'));\n    [...d.querySelectorAll('.btn')].find(b=>b.textContent==='Сказати').click();\n    await new Promise(r=>setTimeout(r,340)); };\n  await say('Добрий день, зараз віддам.');\n  T('репліка потрапила в лог', /Добрий день, зараз віддам/.test(d.querySelector('.log').textContent));\n  T('клієнт відповів', S.history.length===2 && S.history[1].role==='assistant');\n\n  console.log('\\n— панель перевірки рахує чек —');\n  const before=d.querySelector('.tape').textContent;\n  const box=d.querySelector('.mock input[type=checkbox]:not(:checked)');\n  box.checked=true; box.dispatchEvent(new w.Event('change'));\n  const after=d.querySelector('.tape').textContent;\n  T('чек перерахувався після додавання позиції', before!==after);\n  T('стрічка показує приріст', /Приріст розмови/.test(after));\n\n  console.log('\\n— завершення —');\n  for(let i=0;i<3;i++) await say('репліка '+i);\n  T('розмова завершилась після 4 реплік', S.ended===true);\n  T('розбір зʼявився', S.feedback.length>20);\n  T('кнопка виходу відкрилась', !d.querySelector('.hide')||!/hide/.test(\n      [...d.querySelectorAll('.btn')].find(b=>/до списку|Наступне/i.test(b.textContent))?.className||'hide'));\n\n", '  /* Х23.3 (Р1, S44): сум на екрані розмови немає (§9 E2). Суму читаємо з ВИХОДУ застосунку — шапки\n     розбору після кінця («приріст X з Y ₴») — і звіряємо з власною арифметикою над каталогом. */\n  const expBase=S.sc.order.reduce((a,c)=>a+S.ALL[c].b,0);\n  T(\'передумова: сума замовлення не нульова\', expBase>0);\n  T(\'Р1: у чеку екрана розмови немає гривні\', !/₴/.test(d.querySelector(\'.cart-panel .panel\').textContent));\n  const TA=()=>d.querySelector(\'.v2 .composer textarea\');\n  const say=async t=>{ TA().value=t; TA().dispatchEvent(new w.Event(\'input\'));\n    d.querySelector(\'.v2 .send\').click(); await new Promise(r=>setTimeout(r,340)); };\n  await say(\'Добрий день, зараз віддам.\');\n  T(\'репліка потрапила в стрічку\', /Добрий день, зараз віддам/.test(d.querySelector(\'.v2 .messages\').textContent));\n  T(\'клієнт відповів\', S.history.length===2 && S.history[1].role===\'assistant\');\n\n  console.log(\'\\n— панель перевірки рахує чек —\');\n  const rows0=d.querySelectorAll(\'.cart-row\').length;\n  const box=d.querySelector(\'.mock input[type=checkbox]:not(:checked)\');\n  box.checked=true; box.dispatchEvent(new w.Event(\'change\'));\n  T(\'чек перерахувався після додавання позиції\', d.querySelectorAll(\'.cart-row\').length===rows0+1);\n\n  console.log(\'\\n— завершення —\');\n  for(let i=0;i<3;i++) await say(\'репліка \'+i);\n  T(\'розмова завершилась після 4 реплік\', S.ended===true);\n  T(\'розбір зʼявився\', S.feedback.length>20);\n  const fmt=v=>(Math.round(v*100)/100).toFixed(2).replace(\'.\',\',\');\n  const expDelta=S.cart.reduce((a,c)=>a+S.ALL[c].b,0)-expBase;\n  const head=(d.querySelector(\'.talk-review strong\')||{}).textContent||\'\';\n  T(\'шапка розбору: приріст = стало − було (\'+fmt(expDelta)+\')\', S.sc.noSale ? /не міняти/.test(head) : head.includes(\'приріст \'+fmt(expDelta)+\' з\'));\n  T(\'приріст не нульовий — позицію з панелі пораховано\', expDelta>0);\n  T(\'кнопка виходу відкрилась\', !!d.querySelector(\'[data-e2-end="exit"]\'));\n\n'), ('smoke_step1.js', 'зміна: тег «Замовлення 1 з 5»', "  T('усі 5 — інтернет-замовлення', S.shift.queue.every(s=>!!s.no));\n", "  T('усі 5 — інтернет-замовлення', S.shift.queue.every(s=>!!s.no));\n  T('E2 у зміні несе тег «Замовлення 1 з 5»', [...d.querySelectorAll('.case-title .v2-tag')].some(x=>x.textContent==='Замовлення 1 з 5')); /* Х23.3 */\n"), ('smoke_step2.js', 'поле → v2', "  const ta=d.querySelector('.say textarea');", "  const ta=d.querySelector('.v2 .composer textarea'); /* Х23.3 */"), ('smoke_step2.js', 'надіслати → v2', "  [...d.querySelectorAll('.btn')].find(b=>b.textContent==='Сказати').click();\n  await new Promise(r=>setTimeout(r,300));\n\n  const req=", "  d.querySelector('.v2 .send').click(); /* Х23.3 */\n  await new Promise(r=>setTimeout(r,300));\n\n  const req="), ('smoke_step2.js', 'стрічка → v2', "/Добре, беру/.test(d.querySelector('.log').textContent)", "/Добре, беру/.test(d.querySelector('.v2 .messages').textContent)")]
files = {}
for f, n, was, new in PAIRS:
    p = os.path.join(root, 'tools', f)
    files.setdefault(f, open(p, encoding='utf-8').read())
done = [files[f].count(new) == 1 and (was in new or files[f].count(was) == 0) for f, n, was, new in PAIRS]
if all(done):
    print('смоуки Х23.3 уже застосовано — без змін'); sys.exit(0)
assert not any(done), ('часткове застосування', [p[1] for p, x in zip(PAIRS, done) if x])
for f, n, was, new in PAIRS:
    assert files[f].count(was) == 1, ('«було» не рівно раз', f, n, files[f].count(was))
    files[f] = files[f].replace(was, new, 1)
    assert files[f].count(new) == 1, ('«стало» не рівно раз', f, n)
for f, t in files.items():
    open(os.path.join(root, 'tools', f), 'w', encoding='utf-8').write(t)
print('смоуки Х23.3 застосовано:', ', '.join(sorted(files)), '· пар', len(PAIRS))
