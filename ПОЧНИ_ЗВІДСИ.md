# AE-Simulator — з чого почати

Привіт, Олю. Твої сценарії живі — усі 43, разом із каталогом на 181 позицію.
Нижче — що де лежить і що можна міняти самій.

## Що зараз працює

**Клієнт живий.** Його грає модель Qwen через сервіс Groq.

Щоб він заговорив, потрібен ключ. Відкрий **Налаштування** в застосунку і встав
туди ключ із `console.groq.com` → API Keys → Create Key. Натисни
«Зберегти й перевірити» — має з'явитись зелене «✓ Зв'язок є».

Ключ лежить лише в твоєму браузері. Це тимчасово, для перевірки. Пізніше він
переїде на сервер, і поле зникне.

### Режим без витрат

Додай `?mock=1` в кінець адреси сайту — застосунок працюватиме на заглушці,
**не витрачаючи грошей**. Замість клієнта — чотири завчені репліки, зате
з'являється жовта панель, де можна руками скласти чек і подивитись, як
рахується приріст. Зручно, щоб показати комусь інтерфейс.

## Файли, які **можна** редагувати

| Файл | Що це |
|---|---|
| `data/scenarios.json` | твої 43 сценарії |
| `data/catalog.json` | товари і бонуси. Оновлювати щомісяця |
| `prompts/client.md` | **інструкція для моделі — характер клієнта, правила гри** |
| `prompts/debrief.md` | інструкція для розбору цілої зміни |
| `config.json` | налаштування: скільки замовлень у зміні, код куратора, модель |

## Файли, які **не треба** чіпати

`index.html` · `ae_validate.js` · `smoke_step1.js` · `manifest.webmanifest`

Це механіка. Якщо там щось зламати — застосунок перестане відкриватись.

## Правило номер один

У промптах є слова у подвійних дужках: `{{who}}`, `{{mood}}`, `{{catalog}}`.
Це не текст — це **віконця, куди програма підставляє дані сценарію**.
Текст навколо них міняй скільки завгодно. Самі дужки не чіпай і не перейменовуй.

## Після кожної правки — перевірка

Якщо є можливість запустити (або попроси того, хто налаштовував):

```
node ae_validate.js
```

Має бути `✓ підсумок: ✓13 · ⚠1 · ✗0`.
Якщо є хоч один `✗` — застосунок зламається. У повідомленні написано, який
саме сценарій і що в ньому не так.

## Якщо потрібна допомога

Відкрий Claude, прикріпи файл **`HANDOFF_для_Claude.md`** із цієї ж теки і напиши,
що хочеш зробити. Він прочитає і зрозуміє весь проєкт з нуля.
